# Learnings

- 2026-06-04: Task 1 gate completed positively after re-checking PR #18. `DongwonTTuna-Labs/home-server-infra` PR #18 is `MERGED`, `main` SHA is `727f713d2fc497b735ac17b575b74e282b09830e`, and both `.github/workflows/codex-loop-reusable.yml` and `.github/workflows/codex-loop-dispatch.yml` exist on `main`. Consumers should SHA-pin this commit, not a branch.
- 2026-06-04: Task 2 documented Codex loop payload schema v2 in HSI with `schema_version=2`, required explicit state pointers `state_run_id` and `state_artifact_name`, strict `additionalProperties: false`, allowed `review -> design -> fix -> review` and issue/terminal paths, stale-head checkpoints, and `iteration >= max_iterations` no-redispatch semantics. Evidence captured in `.omo/evidence/task-2-schema-keys.txt` and `.omo/evidence/task-2-additionalprops-error.txt`.
- 2026-06-04: Task 2 tracking fix: HSI `.gitignore` rule `**/*pat*` also matches the `pat` substring in `codex-loop-dispatch-payload.v2.schema.json`; keep the PAT-secret rule intact and add the narrow negation `!schemas/*.schema.json` after it so schema JSON deliverables are committable without unignoring PAT/token secrets.

- 2026-06-04: Task 5 aligned HSI Codex loop guards: reusable validation now rejects `iteration >= max_iterations` with `max-iterations-exceeded`, and dispatch/reusable concurrency both key on `codex-loop-${correlation_id}` with `cancel-in-progress: false`. `actionlint` passed on both edited workflows.
- 2026-06-04: Task 3 terminal_reason taxonomy completed despite an aborted subagent: HSI now has `schemas/terminal-reason.v1.json` with 30 canonical underscore reasons and docs table under Loop Termination mapping current hyphenated workflow strings to canonical reasons. Evidence captured in `.omo/evidence/task-3-taxonomy.txt` and `.omo/evidence/task-3-no-label-error.txt`.
- 2026-06-04: Task 4 added a `### Checkout Topology` subsection under `## Trust Boundary` in HSI `docs/codex-loop-reusable.md` (lines 167-182). It defines four worktrees with a Worktree/Repository-ref/Credential-policy/Trust-level/Allowed-actions/Forbidden-actions table: `trusted-core/` (home-server-infra pinned core SHA, trusted CLI install/run, no PR-head exec), `target-base/` (consumer base ref, `persist-credentials: false`, read-only reference), `pr-head/` (consumer head SHA, `persist-credentials: false`, DATA ONLY), `pr-head-write/` (head SHA, fresh checkout only after same-repo/fork/stale guards pass, GitHub App token only, future fix-push). Includes verbatim sentence "PR head is data only; never execute PR-head scripts, actions, package managers, or helper installation under secrets." and a CLI SHA sourcing rule: core sources `codex-review` from the same pinned `home-server-infra` SHA as the workflow (`github.workflow_ref` parsed or explicit `core_sha`), branch pin / moving ref forbidden for production. Evidence in `.omo/evidence/task-4-topology.txt` and `.omo/evidence/task-4-sha-only.txt`; `git diff --check` clean.

- 2026-06-04: Task 6 extended HSI `tests/workflows/test_workflow_contracts.py` (only file edited; no workflow/doc/schema/source edits). Added shape tests (reusable `on.workflow_call`, 7 required typed inputs + optional `max_iterations`/`dry_run`, GitHub App secrets `CODEX_GITHUB_APP_ID`/`CODEX_GITHUB_APP_PRIVATE_KEY`, no `write-all` across all 3 workflows, dispatch `repository_dispatch` exactly `types: [codex-loop]`), safety tests (no `pull_request_target`+`labeled`/`github.event.label`/internal label names in workflows; no PAT/`PERSONAL_ACCESS_TOKEN` secret wiring in workflows+reusable doc; docs show only `@<sha-or-tag>` placeholder pins, no branch pins) with inline negative samples, payload schema v2 positive + 7 negatives (invalid stage, missing head_sha, additional key, malformed head_sha, non-integer iteration, missing state pointer, invalid schema_version), and logical tests (`INPUT_ITERATION >= INPUT_MAX_ITERATIONS`, terminal-reason enum has `stale_head`/`fork_pr`/`max_iterations`, docs describe stale-head/fork/state_run_id/state_artifact_name). Full suite 19->42 passing; negative subset 15 passing. Evidence: `.omo/evidence/task-6-pytest.txt`, `.omo/evidence/task-6-negative.txt`.
- 2026-06-04: Task 6 tooling note: `jsonschema` is NOT importable under `uvx pytest` (no pyproject/deps in HSI), so payload v2 tests use a dependency-free JSON-Schema-subset validator that loads the real `schemas/codex-loop-dispatch-payload.v2.schema.json` (supports type/additionalProperties:false/required/const/enum/pattern/minimum/minLength/maxLength). Integer type check excludes Python bool. LSP `basedpyright` not installed; validated with `python3 -m py_compile` + `git diff --check` clean. PAT guard must avoid the "dispatch"/"pat" substring trap: it matches `personal_access_token` (i), `secrets.*PAT*`, `\bGH_PAT\b` only, so doc prose "PAT fallback is forbidden" is not flagged.

- 2026-06-04: Task 7 copied the full RS `setup/codex-review/` helper package into HSI `setup/codex-review/` excluding generated caches only. HSI `.gitignore` needed a narrow `!setup/codex-review/**` negation because package paths intentionally contain `token`, `secret`, and `pat` substrings in security tests and patch/dispatch modules. HSI reusable workflow now installs `codex-review` from `trusted-core/setup/codex-review` after checking out `DongwonTTuna-Labs/home-server-infra` at `${{ github.workflow_sha }}`, preserving the trust-boundary rule that PR head is data only. Evidence: `.omo/evidence/task-7-install.txt`, `.omo/evidence/task-7-schema-path-error.txt`.

- 2026-06-04: Task 7 cleanup fixed the broad package unignore side effect by adding generated-artifact re-ignore rules after `!setup/codex-review/**` for `__pycache__/`, `*.pyc`, `*.pyo`, and `src/*.egg-info/`. Removed only generated artifacts from HSI `setup/codex-review/`, re-ran install/help/import/schema smoke checks, then removed smoke-regenerated caches/egg-info again. Final HSI probes found no generated artifacts in `git status --short --untracked-files=all` and none on disk, while `git add -n` confirmed key package files remain addable, including `bin/codex-review`, `pyproject.toml`, `github/app_token.py`, `patches/commit_plan.py`, `cli/handlers/fix_dispatch.py`, and `schemas/review-axis-findings.v1.schema.json`.

- 2026-06-04: Task 8 made HSI `setup/codex-review/config.yml` consumer-overridable. `load_config()` now takes keyword-only `override` (mapping, highest precedence) and `override_path` (explicit file, strict-must-exist) plus an ambient `CODEX_REVIEW_CONFIG_OVERRIDE` env var (best-effort, missing file = no override, no crash); merge order is generic `DEFAULT_CONFIG` -> base `config.yml` -> override file -> override mapping via the existing `_deep_merge`. A missing base `config.yml` at the default location now falls back to `DEFAULT_CONFIG` instead of raising, but an explicit `path=` that does not exist still raises (backward compatible). New strict CLI flag `--config-override` is wired in `cli/_helpers.py` (`_add_common`) and `cli/__init__.py` (`load_config(args.config, override_path=args.config_override)`); positional `load_config(path)` and `--config` are unchanged. `config.yml` was genericized: RS-specific values (`trusted.user=DongwonTTuna`, RS bot authors, `autofix.forbidden_files=[Cargo.toml,Cargo.lock]`, Polymarket `dangerous_keywords` like calldata/eip-712/wallet/wallet-create/wire/serde, cargo `tests.allowlist`/`default_ids`) were removed; behavioral guards (autofix numeric thresholds, review-infra `forbidden_prefixes`, `allowed_prefixes`, design batch size) were preserved because unit tests depend on them. The RS values were relocated (not deleted) to `tests/fixtures/config/consumer_override.yml` which doubles as the override test fixture and documents the consumer `.codex-loop.yml` shape. New tests in `tests/unit/test_config_override.py` (13) cover deep-merge, env-lenient fallback, explicit-missing raise, generic-defaults, secrets-not-required, RS-paths-not-mandatory, and missing-base fallback. Evidence: `.omo/evidence/task-8-override.txt`, `.omo/evidence/task-8-default-fallback.txt`. Full unit suite: 217 passed (no override = generic defaults verified through both the Python API and the CLI dispatcher with rc=0).
- 2026-06-04: Task 9 removed active HSI loop memory transport through sticky PR comments. `loop/state.py` now exposes artifact/payload helpers (`empty_loop_state`, `read_loop_state_payload`, `read_loop_state_artifact`, `write_loop_state_artifact`), `loop read-state` reads only explicit `--loop-state`/`--in` JSON or an input payload and defaults to empty state for missing artifacts, and `push record-push` emits the updated `loop-state.v1` JSON directly for upload-artifact. Reentry records now mark pushed loop state as `persist_transport=artifact`, and resolve-gate apply includes artifact-ready `loop_state` in its JSON report instead of writing a loop-state comment. Focused tests added artifact load/default/read/write and monkeypatched no-comment read/write assertions. Evidence: `.omo/evidence/task-9-state.txt`, `.omo/evidence/task-9-no-comment-state-error.txt`.

- 2026-06-04: Task 10 aligned HSI cross-run loop-state artifact acquisition to explicit `state_run_id` + `state_artifact_name` transport. The relocated helper now validates state pointers and constructs `gh run download <run_id> --name <artifact> --dir <out>` without any head-SHA lookup; dispatch/manual/reusable workflows require and forward the same fields, and reusable downloads with `actions/download-artifact@v4` using `run-id`. Focused evidence: `.omo/evidence/task-10-runid.txt` and `.omo/evidence/task-10-no-headsha-error.txt`.

- 2026-06-04: Task 11 closed the Wave-2 pytest gate by realigning the relocated HSI `setup/codex-review/tests/workflow/` suite from the obsolete RS split-workflow topology (`codex-review.yml`/`codex-design.yml`/`codex-fix.yml`/`codex-issue.yml` + `setup-codex-review` composite action) to the current reusable/dispatch/manual topology. Baseline was `35 failed, 244 passed`; after realignment `281 passed`. Edited 5 test files only (no workflow/source/doc/schema edits): rewrote `_pipeline.py` (PIPELINE_FILENAMES now `codex-loop-reusable.yml`/`codex-loop-dispatch.yml`/`codex-loop-manual.yml`, CODEX_ACTION `openai/codex-action@v1`, added REUSABLE/DISPATCH/MANUAL/reusable_jobs helpers); rewrote `test_orchestrator_shape.py` (kept 31 funcs) to assert the reusable job order `validate -> trust-and-stale-guard -> setup-relay -> run-stage -> finalize`, concurrency `codex-loop-${{ inputs.correlation_id }}` cancel-in-progress:false, stage-result JSON parse/validation, trusted-core checkout at `github.workflow_sha` with persist-credentials:false, codex-action relay wiring (relay token + responses endpoint, no codex-args/AI_RELAY_API_KEY), fork-pr/stale/owner/requester trust guards, explicit state_run_id transport, dry-run-default write gating, and redispatch-terminal-until-fix-push; rewrote `test_permissions.py` (7->9 funcs) to assert top-level `permissions: {}`, every non-finalize job read-only on repo (id-token:write only for setup-relay/run-stage), finalize as the only repo-write job, adapter ceilings, no write-all, no PAT/app-token/writable GITHUB_TOKEN, no issues:write; realigned `test_no_inline_python_or_schema_bloat.py` (bin-path count -> trusted-core console-entrypoint invariants) and `test_no_resolve_checker_workflow.py` (resolve-gate-in-split-workflow -> stage-integrated-in-reusable-core). Net test funcs 48->50, zero deletions, no test file removed. Verified: `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q` => 281 passed; `actionlint` clean on all 3 workflows; `git diff --check` clean on touched files; no generated Python artifacts in git status. Evidence: `.omo/evidence/task-11-pytest.txt`, `.omo/evidence/task-11-no-test-deletion.txt`.

- 2026-06-04: Task 11 tooling note: the bare acceptance command `uvx pytest setup/codex-review/tests -q` cannot pass because `uvx` builds an isolated tool env that does not install the relocated package's declared runtime deps (`PyYAML`/`jsonschema`/`cryptography` from `setup/codex-review/pyproject.toml`), so collection aborts with `ModuleNotFoundError`. The honest, repeatable equivalent is `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q`, which installs the package + its own declared deps transitively (15 packages) and goes green. Also: contrary to the Task 7 note that `uvx actionlint` is unavailable, an `actionlint` 1.7.12 binary IS installed locally at `/opt/homebrew/bin/actionlint` and was used as the workflow safety gate.

- 2026-06-04: Task 12 replaced the HSI reusable core's single `run-stage`/`finalize` shape with explicit `review-stage`, `design-stage`, `fix-stage`, `issue-stage`, and `finalize-stage` jobs. `setup-relay` is now the common front job that checks out `trusted-core/` at `${{ github.workflow_sha }}`, `target-base/` at `${{ inputs.base_ref }}`, and `pr-head/` at `${{ inputs.head_sha }}` with `persist-credentials: false`; only `trusted-core/setup/codex-review` is installed/executed. Stage skeletons are deterministic JSON placeholders with stage-specific terminal reasons, not the PR#18 generic prompt or `dry-run-placeholder`. Evidence: `.omo/evidence/task-12-skeleton.txt` and `.omo/evidence/task-12-no-generic-error.txt`.

- 2026-06-04: Task 13 wired the review stage internals by replacing the single `review-stage` skeleton with a four-job pipeline gated on `inputs.stage == 'review'`: `review-collect` (deterministic `resolve_gate route` over empty inventory/lifecycle => `run_review`, emits `route` output), `review-axes` (real `strategy.matrix` `axis: [correctness, security, performance, test-coverage, domain]` + `fail-fast: false`, each axis runs `codex-review review default-result` and uploads `codex-loop-review-<axis>-<corr>-<iter>`), `review-combine` (downloads `codex-loop-review-*` and runs `codex-review review combine`), `review-techlead` (`techlead default-result` -> `classify` -> `publish --dry-run` -> `loop route-after-techlead`, normalizes route into the same `next_stage/lgtm/should_redispatch/terminal_reason` jq contract Task 12 used, uploads `codex-loop-review-state-*`). `finalize-stage` now consumes `needs.review-techlead.outputs.*`. Job order is `validate, trust-and-stale-guard, setup-relay, review-collect, review-axes, review-combine, review-techlead, design-stage, fix-stage, issue-stage, finalize-stage`. Evidence: `.omo/evidence/task-13-review-axes.txt`, `.omo/evidence/task-13-dryrun-posting.txt`.

- 2026-06-04: Task 13 KEY discovery: the relocated `codex-review` package ships purpose-built deterministic, model-free CLI fallbacks (`review default-result`, `review combine`, `techlead default-result`, `techlead classify`, `techlead publish --dry-run`, `loop route-after-techlead`, `resolve_gate route`). This let the dry-run review pipeline run the REAL trusted-core CLI end-to-end with no relay token / no `openai/codex-action` (still forbidden by `test_stage_skeleton_jobs_do_not_use_generic_model_prompt`), staying deterministic and locally verifiable. Verified the full chain in a throwaway venv (`pip install -e setup/codex-review`): 5x `review default-result` -> `review combine` (finding_count 0) -> `techlead default-result` (status lgtm) -> `classify` -> `publish --dry-run` (`dry_run: true`, `commit_id: null`, no PR review) -> `route-after-techlead` => `stop_lgtm`. HSI `dry_run` semantics differ from upstream: dry_run => `setup-relay` skips the OIDC relay token, so the model genuinely cannot run; live model inference + posting are reserved for a later live-capable PR. `review validate` is intentionally NOT wired in the skeleton because it requires `inspection_evidence` paths (e.g. `AGENTS.md`) to exist under `--repo-path`, which is a runtime dependency on the consumer checkout that the deterministic dry-run path should not assume.

- 2026-06-04: Task 13 test realignment (Task 12 tests asserted a single `review-stage` skeleton): updated `tests/workflow/test_orchestrator_shape.py` (new `REVIEW_STAGE_JOBS`/`SKELETON_STAGE_JOBS`/`ALL_STAGE_JOBS`/`REVIEW_AXES` constants; rewrote job-order, stage-gate, no-generic-prompt, responses-endpoint, install-count `== 1+len(REVIEW_STAGE_JOBS)` = 5, gated-skeleton, and intra-workflow-handoff tests; matrix fan-out documented as the one job that hands off via scoped per-axis artifacts not job outputs; added `test_review_axes_matrix_fans_out_over_all_five_axes`, `test_review_pipeline_combines_axes_and_routes_through_techlead`, `test_review_posting_is_disabled_in_dry_run_and_routes_via_artifacts`), `tests/workflow/test_permissions.py` (expanded `STAGE_JOBS` to the four review jobs + three skeletons; all stay `contents:read`/`pull-requests:read`, no `id-token: write`, no app token, no `issues: write`), and `tests/workflow/test_no_resolve_checker_workflow.py` (review portion now asserts the four review job display names instead of "Review Stage Skeleton"). No tests deleted, no skips. Relocated suite 281 -> 284 passed; the two acceptance files (`test_orchestrator_shape.py` + `test_permissions.py`) 43 passed; `actionlint` 0 errors; `git diff --check` clean; no generated python artifacts in HSI `git status`.

- 2026-06-04: Task 14 wired the design stage internals, replacing the single `design-stage` skeleton with a four-job stage-gated pipeline (`inputs.stage == 'design'`): `design-prepare` (downloads the prior review loop-state by EXPLICIT `state_run_id`/`state_artifact_name`, `loop read-state` -> `design context` -> `design default-inventory` -> `design default-clusters` -> `design prepare-analysis-matrix`; emits `analysis_matrix`/`has_analysis_batches` step outputs and uploads `codex-loop-design-prepare-<corr>-<iter>`), `design-analyze` (DYNAMIC `strategy.matrix: ${{ fromJson(needs.design-prepare.outputs.analysis_matrix) }}`, `fail-fast: false`, gated on `has_analysis_batches == 'true'`, runs `design default-analysis --inventory <batch_file>` per batch, uploads `codex-loop-design-analysis-<batch_path>-<corr>-<iter>`), `design-plan` (`if: always() && design-prepare success && (design-analyze success|skipped)`; `design collect-analyses` -> `design default-plan`; uploads `codex-loop-design-plan-<corr>-<iter>`), `design-chief` (final output job analogous to `review-techlead`: assembles `design-state-bundle/` with `design-plan.json`, `design_chief default-result` -> `design_chief publish --dry-run` (posting-disabled guard `jq -er '.dry_run' == true`) -> `design_chief route`, normalizes route into the same `next_stage/lgtm/should_redispatch/terminal_reason` jq contract, uploads `codex-loop-design-state-<corr>-<iter>`). `finalize-stage` now consumes `needs.design-chief.outputs.*`. Job order: validate, trust-and-stale-guard, setup-relay, review-collect, review-axes, review-combine, review-techlead, design-prepare, design-analyze, design-plan, design-chief, fix-stage, issue-stage, finalize-stage. Evidence: `.omo/evidence/task-14-design.txt`, `.omo/evidence/task-14-no-headsha.txt`.

- 2026-06-04: Task 14 KEY discovery: like the Task 13 review fallbacks, the design stages ship deterministic model-free CLI commands (`design context`, `design default-inventory`, `design default-clusters`, `design prepare-analysis-matrix`, `design default-analysis`, `design collect-analyses`, `design default-plan`, `design_chief default-result`, `design_chief publish --dry-run`, `design_chief route`). Verified the full chain in a throwaway venv (`pip install -e setup/codex-review`): `loop read-state` (empty loop-state) -> `design context` (findings=[]) -> `default-inventory` (items=0) -> `default-clusters` (clusters=0) -> `prepare-analysis-matrix` (writes `has_analysis_batches=false`, `analysis_matrix={"include":[]}` to GITHUB_OUTPUT) -> `collect-analyses` (analyses=0) -> `default-plan` (empty edit_sequence) -> `design_chief default-result` (`status: no_fix_needed`, since empty edit_sequence) -> `publish --dry-run` (`dry_run: true`, no comment) -> `route` => `stop_noop`. The design route maps: `run_fix_dispatch`->next_stage=fix/should_redispatch=true; `stop_needs_human`->needs_human; `stop_noop`->`no_fix_needed` (canonical reason in `schemas/terminal-reason.v1.json`); `*`->`design_route_unrecognized`. Critical linchpin reused from Task 13: `validate_inspection_evidence` with `repo_path=None` only requires repo-relative paths (no file-existence check), so `design default-plan` (which calls `validate_design_plan(plan, ctx, config)` with repo_path=None) and the AGENTS.md inspection_evidence fallback work WITHOUT any PR-head checkout. Dynamic `matrix: ${{ fromJson(...) }}` with empty include + the `has_analysis_batches == 'true'` `if` gate mirrors the legacy `codex-design.yml` analyze_clusters and is accepted by `actionlint` (exit 0).
- 2026-06-04: Task 15 wired HSI fix stage internals as a dry-run/no-push three-job pipeline: `fix-dispatch` downloads the design state bundle by explicit `state_run_id`/`state_artifact_name`, runs `fix_dispatch plan` + `prepare-agents`, and emits the real `agent_matrix`; `fix-run-agents` is a real `strategy.matrix` fan-out that emits deterministic `default-agent-result` artifacts without relay/model execution; `fix-merge-validate` fans in agent artifacts, runs `fix_dispatch collect`, `fix_merge premerge/prepare-merge-model/default-merged-fix/validate`, deterministic semantic-safety fallback, `push validate-fix --dry-run`, `push check-loop-budget`, and uploads `codex-loop-fix-state-<corr>-<iter>` containing `merged-fix.json`, `validated-fix.json`, `semantic-safety.json`, and `manifest.json`. Evidence: `.omo/evidence/task-15-fix.txt`, `.omo/evidence/task-15-no-push-error.txt`. Verified: `actionlint` clean; focused workflow tests 51 passed; full relocated suite 292 passed; negative push/dispatch grep no matches; no generated Python artifacts in HSI status.
- 2026-06-04: Task 15 follow-up fixed the `fix-dispatch` dynamic matrix runtime output bug. `plan_tasks` now writes compact `agent_matrix=$(jq -c . codex-review-artifacts/fix_dispatch/agent-matrix.json)` and boolean `has_agent_tasks` to `${GITHUB_OUTPUT}` immediately after `fix_dispatch prepare-agents`, while keeping the step summary. `test_orchestrator_shape.py` now inspects the `plan_tasks` shell body for both output writes so mapping-only coverage cannot pass if the producer stops emitting outputs. Reverified: actionlint pass, focused workflow tests 51 passed, full relocated suite 292 passed, no push/dispatch grep matches, diff checks clean, no generated Python artifacts in HSI status.

- 2026-06-04: Task 16 wired HSI `stage=issue` as an artifact-only terminal fallback, replacing the final issue skeleton with `Issue Fallback Artifact`. The job checks out/installs only `trusted-core/setup/codex-review`, synthesizes trusted PR context from workflow inputs, runs `codex-review issue_fallback infer-reason -> plan -> compose`, uploads `codex-loop-issue-fallback-<correlation_id>-<iteration>`, writes summary fields (`fallback_reason`, title, body excerpt), and normalizes terminal outputs to `next_stage=""`, `lgtm=false`, `should_redispatch=false`, `terminal_reason=issue_fallback_artifact_only`. It intentionally does not run `issue_fallback apply`, grant `issues: write`, use app tokens, or use labels/comments/issues as state. Evidence: `.omo/evidence/task-16-issue.txt`, `.omo/evidence/task-16-no-issue-state.txt`. Verified: actionlint clean, focused workflow tests 52 passed, full relocated suite 293 passed, CLI artifact driver produced `artifacts_missing` planned content.

- 2026-06-04: Task 17 normalized HSI `finalize-stage` and reusable outputs to the seven-key route/state contract: `next_stage`, `lgtm`, `should_redispatch`, `terminal_reason`, `state_run_id`, `state_artifact_name`, `updated_head_sha`. The finalizer now writes all seven keys, sets `state_run_id` from `github.run_id`, computes deterministic stage artifact pointers for review/design/fix/issue, uses `codex-loop-state-<corr>-<iter>.json` only when setup succeeded but the selected stage failed, leaves validation/trust/setup failure artifact pointers empty rather than claiming missing artifacts, and keeps `updated_head_sha` as an empty placeholder. Dry-run always forces `should_redispatch=false` and uses canonical `terminal_reason=dry_run` when no terminal reason exists. Terminal reason literals were canonicalized to `schemas/terminal-reason.v1.json` values, so old spellings such as `dry-run`, `fork-pr`, and `issue_fallback_artifact_only` should not reappear. Evidence: `.omo/evidence/task-17-finalize.txt`, `.omo/evidence/task-17-dryrun-redispatch.txt`. Verified: actionlint clean, focused workflow tests 55 passed, full relocated suite 296 passed, diff checks clean, no generated Python artifacts in HSI status.

- 2026-06-04: Task 18 standardized HSI state-bundle transport + integrity. New single-source-of-truth contract `setup/codex-review/src/codex_review/loop/bundle.py` (`BUNDLE_CONTRACTS`, `infer_bundle_kind`, `verify_bundle`) defines required files per bundle kind: review=[techlead-decision, review-publication, publish-report, route], design=[design-plan, chief-decision, publish-report, route], fix=[manifest, merged-fix, validated-fix, semantic-safety], issue=[reason, plan, issue-fallback], loop-state=[.codex-loop-state.json]. `verify_bundle` returns a `loop-state-bundle-verify.v1` verdict: a missing required file -> `artifact_missing` (missing beats invalid), corrupt/non-JSON required file -> `artifact_schema_invalid`, unknown kind -> `artifact_missing` (never silent success). Bootstrap exception is opt-in only (`allow_initial_empty=True` AND an entirely empty bundle); a partial bundle is never bootstrapped. New CLI `codex-review loop verify-bundle --kind <auto|review|design|fix|issue|loop-state> --name <artifact> --dir <dir> --out <verdict.json>` (new global args `--dir`/`--kind`/`--allow-initial-empty` in `cli/_helpers.py`). Workflow wiring: `design-prepare` (kind review) and `fix-dispatch` (kind design) download the prior bundle with `continue-on-error: true`, run a `verify_bundle` step that publishes `bundle_ok`/`bundle_terminal_reason` job outputs; productive steps + downstream `design-plan`/`fix-merge-validate` gate on `bundle_ok == 'true'`, so a bad bundle skips the output job. `finalize-stage` now `needs` design-prepare+fix-dispatch and, when the selected stage's output job is non-success, sets `terminal_reason` from the consumer's `bundle_terminal_reason` (else keeps `stage_failed`); the existing canonical-enum guard and dry-run handling are preserved so missing bundles surface `artifact_missing` even in dry-run while genuine stage crashes stay `stage_failed`. dispatch-ledger.json is recorded as a deferred carry-forward (Task 23) and is never required. Evidence: `.omo/evidence/task-18-bundle.txt`, `.omo/evidence/task-18-missing.txt`. Verified: actionlint clean; focused `-k "bundle or artifact"` 48 passed; `-k "artifact_missing or missing_bundle"` 6 passed; workflow-shape suite 68 passed; full relocated suite 296->317 passed; finalize run body extracted from YAML and executed across 5 scenarios (design/fix missing-bundle dry-run+live -> artifact_missing, genuine failure -> stage_failed, happy design->fix dry-run -> dry_run); `git diff --check` clean; no tracked/untracked generated Python artifacts.

- 2026-06-04: Task 18 KEY design decision: existing `setup/codex-review/tests/workflow/test_orchestrator_shape.py` pins the reusable job set EXACTLY (`test_reusable_core_declares_expected_job_order`) and pins `design-prepare`/`fix-dispatch`/`fix-merge-validate` `needs` lists exactly, so bundle integrity could NOT be a new job and could NOT change consumer `needs`. It was implemented as in-job verify STEPS + job OUTPUTS + downstream `if` gates, with the missing-bundle terminal reason propagated through `finalize-stage` (whose `needs` is NOT exact-asserted, so design-prepare/fix-dispatch were safely added there). The consumer job intentionally SUCCEEDS with `bundle_ok=false` (not fail) so its outputs reliably propagate to finalize regardless of GitHub's failed-job-output semantics; the loop OUTCOME is still terminal `artifact_missing` (no lgtm, no redispatch), satisfying "do not treat missing bundles as empty success." Downloads keep explicit `run-id: ${{ inputs.state_run_id }}` + `name: ${{ inputs.state_artifact_name }}`; `continue-on-error: true` on the download converts a 404/absent artifact into a verifiable empty dir -> artifact_missing rather than an opaque stage_failed.

- 2026-06-04: Task 18 FOLLOW-UP FIX (orchestrator caught a real gap in the first pass): the common `setup-relay` job runs for every stage and its "Download prior loop state artifact" step lacked `continue-on-error`, so a missing prior STAGE bundle hard-failed setup-relay BEFORE design-prepare/fix-dispatch could run verify-bundle. Because every stage job gates on `needs.setup-relay.result == 'success'`, the stage verifier never ran and finalize normalized the run to `stage_failed` (SETUP_RESULT!=success branch) instead of `artifact_missing`. The first-pass finalize manual QA missed this because it simulated `SETUP_RESULT=success`; in reality SETUP failed first. Fix = add `continue-on-error: true` (+ `id: download_prior_state`) to setup-relay's prior-state download ONLY (explicit `run-id`/`name` preserved). `read-state` already tolerates an absent path -> empty loop-state, and the fresh `.codex-loop-state.json` upload still succeeds, so setup-relay now SUCCEEDS on a missing prior bundle and the design/fix verify-bundle gate becomes the single integrity authority that emits `artifact_missing` through finalize. Crucially, ONLY the download step is non-blocking: the relay-token mint step still hard-fails, so genuine setup failures keep canonical reasons (relay mint -> `stage_failed`, missing app creds -> `missing_app_credentials`), and validate/trust failures keep `validation_failed`/`fork_pr` — verified by executing the extracted finalize run body across 8 cases (A/B/C missing-bundle dry+live -> artifact_missing; D/D2 setup failures -> stage_failed/missing_app_credentials; E trust -> fork_pr; F happy -> dry_run; G validate -> validation_failed) using a GHA-mirrored env that defines all 36 finalize env vars (so `set -u` matches production where `${{ }}` resolves to ""). Added regression test `test_setup_relay_prior_state_bundle_download_is_non_blocking` asserting `continue-on-error: true` + preserved run-id/name (would have caught the original gap). Verified: actionlint clean; `-k "bundle or artifact"` 48->49 passed; `-k "artifact_missing or missing_bundle"` 6 passed; workflow-shape 68->69 passed; full relocated suite 317->318 passed; `git diff --check` clean; no generated Python artifacts. Evidence appended to `.omo/evidence/task-18-bundle.txt` and `.omo/evidence/task-18-missing.txt`.


- 2026-06-04: Task 19 added HSI `setup/codex-review/tests/unit/test_stage_cli_schema_parity.py` (ONLY new file; no schema/prompt/stage/workflow edits) proving the reusable-loop deterministic dry-run stage commands (Tasks 13-15) emit artifacts conforming to the existing v1 schemas. It drives the real `codex-review` CLI via `from codex_review.cli import main` (the same console entrypoint the workflow invokes) and validates every emitted artifact with `jsonschema.validate(payload, load_schema_json(name))` — using jsonschema directly (NOT the library's silently-noop `validate_json_schema`) so a missing dep fails loudly, making schema-shape parity a hard gate. 8 parity tests, all green; full relocated suite 318->326 passed. Coverage: review = `review default-result` x5 axes (review-axis-findings.v1) + `review combine` (review-combined-findings.v1) + `techlead default-result` (techlead-decision.v1) + `techlead classify` (techlead-review-publication.v1), both empty(lgtm) and populated(2 synthetic axis findings flowing through combine/classify) chains; design = full deterministic chain `design context`->`default-inventory`->`default-clusters`->`default-analysis`->`default-plan`->`design_chief default-result`->`design_chief route` validating design-context/inventory/clusters/cluster-analysis/plan/chief-decision .v1, both empty(no_fix_needed/stop_noop) and populated-openspec(non-empty edit_sequence -> needs_human/stop_needs_human); fix = `fix_dispatch plan`(fix-dispatch-task-manifest.v1)->`prepare-agents`(dynamic matrix include)->`default-agent-result`+`validate-agent-result`(fix-dispatch-agent-result.v1, no_safe_fix)->`collect`(fix-dispatch-collection-result.v1) plus `fix_merge default-merged-fix`(fix-merge-merged-fix.v1, no_fix)->`push validate-fix --dry-run`(push-validated-fix.v1, pushed=false)->`push check-loop-budget`. Evidence: `.omo/evidence/task-19-parity.txt`, `.omo/evidence/task-19-schema-diff.txt`.
- 2026-06-04: Task 19 KEY: HSI `setup/codex-review` package is currently entirely UNTRACKED in git (uncommitted migration baseline from Tasks 7+); `git ls-files setup/codex-review/schemas setup/codex-review/prompts` == 0. So `git diff --stat -- schemas prompts` is empty by construction, and the meaningful "no schema/prompt change" proof is that this task's only working-tree delta is the single new parity test file (`git status --short` shows exactly `?? .../test_stage_cli_schema_parity.py`), plus `git status --untracked-files=no -- setup/codex-review` empty (zero tracked modifications). Verification command per Task 11 note: bare `uvx pytest` cannot install the relocated package deps, so use `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q [-k parity]`. `-k parity` selects all 8 because the module name contains "parity".
- 2026-06-04: Task 19 dry-run scope (documented in test docstrings, not silently skipped): Wave 3 deterministic outputs assert schema SHAPE parity, not byte-exact legacy content, because dry_run means no relay token -> no model -> deterministic `default-*` fallbacks. The `push validate-fix --dry-run` deterministic path yields `status: no_fix` (no model patch), which short-circuits before any git/network call; a populated `status: dry_run` validated artifact requires a model-produced patch applied in a PR-head git checkout and is deferred to the live-capable change (Task 13/15 OPEN boundary). Issue stage is explicitly pinned as artifact-only terminal fallback (Task 16, schema_version `issue-fallback-issue-fallback.v1`) and asserted to be OUTSIDE the review/design/fix legacy v1 parity set — documented, not omitted.

- 2026-06-04: Task 20 wired HSI trusted fix-push as an explicit two-phase path: `fix-merge-validate` now performs no-token `push validate-fix` against read-only `pr-head`, and new `fix-push` runs only for non-dry-run trusted same-repo PRs after guard success, checks out fresh `pr-head-write` at exact `inputs.head_sha`, mints a GitHub App installation token with `--mode push`, revalidates live PR head inside `commit-push`, reapplies only the validated fix artifact, commits, and pushes. Successful push now returns `next_stage=review`/`should_redispatch=true` to the caller without implementing repository_dispatch; Task 21 `updated_head_sha` capture and Task 22 dispatch remain deferred.

- 2026-06-04: Task 21 made HSI `updated_head_sha` a remote-verified output. `commit_and_push_validated_fix` (setup/codex-review/.../stages/push/orchestrate.py) now sets `updated_head_sha = commit_sha` ONLY in the `pushed and verified` branch (empty for `pushed_unverified`/`push_failed`) and surfaces `remote_head_sha` at top level; `verified` is true only after `push_commit`'s `git ls-remote` confirmed `remote_head_sha == commit_sha` or `verify_pushed_head` confirmed the PR-head API equals `commit_sha`, so the exported value is remote-confirmed, never local-only. Workflow `fix-push` reads `.verified` + `.updated_head_sha` from push-result.json and a successful push is GATED on `verified == true && non-empty updated_head_sha` (else routes `pushed_unverified`); it emits `fix-push.outputs.updated_head_sha`, and `finalize-stage` consumes `FIX_PUSH_UPDATED_HEAD_SHA` to fill the reusable `updated_head_sha` workflow_call output (Task 17 empty placeholder is gone for the verified-push path). Task 22 dispatch and `record-push` remain intentionally absent. Evidence: `.omo/evidence/task-21-updated-head.txt`, `.omo/evidence/task-21-unverified.txt`.
- 2026-06-04: Task 21 KEY discovery: `commit_sha` IS a remote-verified value in the `verified` branch by construction. Both verification paths confirm it against the remote: `push_commit`/`verify_remote_head_ref` compares ls-remote `refs/heads/<head_ref>` to `expected_sha` (= `git rev-parse HEAD` after committing = commit_sha), and the fallback `verify_pushed_head` compares the GitHub PR-head API to commit_sha. So setting `updated_head_sha = commit_sha` only when `verified` satisfies "remote-confirmed, not local-only". The unit test `test_commit_push_pushed_unverified_withholds_local_commit_sha` makes the remote gate observable: push returns rc=0 but `remote_head_sha` differs and the API check also fails -> `updated_head_sha == "" != commit_sha`; it FAILS if a local SHA is trusted without confirmation.
- 2026-06-04: Task 21 verification: focused `-k "updated_head or remote_head"` 4 passed, `-k "pushed_unverified"` 2 passed, fix-push/finalize workflow-shape subset 12 passed, full relocated suite 335 passed; `actionlint` clean on all three codex-loop workflows; `git diff --check` rc=0; generated py artifacts (`__pycache__`/`*.pyc`/`egg-info`) are git-ignored and were removed from disk. Manual QA drove a REAL push to a local bare remote (only App-token identity checks stubbed; `push_commit` + `git ls-remote` ran for real): `updated_head_sha == commit_sha == independent bare-repo refs/heads/feature/x`, != base head. A bash mirror of the fix-push normalize/parse steps confirmed: verified -> `next_stage=review`/`should_redispatch=true`/non-empty head; attack (status=pushed but verified=false with smuggled local SHA) -> gate fires -> `pushed_unverified`/no redispatch/empty head; canonical `pushed_unverified` -> empty head.
- 2026-06-04: Task 22 added live `repository_dispatch` continuation inside HSI `finalize-stage`: after final outputs resolve to `should_redispatch=true` and `inputs.dry_run == false`, the workflow checks out trusted core, builds a deterministic `jq -n` `client_payload`, mints a fresh GitHub App installation token with new minimal `auth app-token --mode dispatch` (`contents:write`, `pull_requests:read`), and posts `event_type=codex-loop` to `/repos/${REPOSITORY}/dispatches` with `Authorization: Bearer ${DISPATCH_APP_TOKEN}`. Payload includes `schema_version`, `next_stage`, adapter-compatible `stage`, `head_sha`, `base_ref`, `iteration=current+1`, `correlation_id`, `state_run_id`, `state_artifact_name`, `requested_by`, `dry_run`, `max_iterations`, and `pr_number`. Fix-stage continuation replaces input head with verified `updated_head_sha` and fails before dispatch if that value is absent. Evidence: `.omo/evidence/task-22-dispatch.txt`, `.omo/evidence/task-22-guard.txt`.

- 2026-06-04: Task 23 promoted HSI `dispatch-ledger.json` from Task 18 deferred carry-forward to artifact-backed loop state. `loop-state.v1` now normalizes `dispatch_ledger`; new CLI commands `codex-review loop append-dispatch-ledger` and `guard-dispatch` operate on deterministic logical entries (`correlation_id`, `stage`, `iteration`, `head_sha`, `state_run_id`, `state_artifact_name`) and return canonical terminals before the live dispatch API path: duplicate logical key -> `dispatch_duplicate`, `iteration >= max_iterations` -> `max_iterations`, per-correlation cap -> `max_iterations`, repeated same stage/head signature -> `oscillation_detected`. Continuation-capable bundles (review/design/fix/fix-push) now carry `dispatch-ledger.json`; setup/design/fix paths import it from explicit state artifacts, and `finalize-stage` downloads the selected state bundle and runs `guard-dispatch` before minting the dispatch App token or calling `/dispatches`. Reusable and repository_dispatch adapter concurrency remain unified on `correlation_id`. Evidence: `.omo/evidence/task-23-ledger.txt`, `.omo/evidence/task-23-concurrency.txt`. Verified: py_compile, actionlint, focused ledger/duplicate/oscillation/max_iterations tests (35 passed), focused workflow dispatch/concurrency/fix_push tests (14 passed), full relocated suite (349 passed), git diff check, and generated-artifact scan clean.

- 2026-06-04: Task 23 verification fix corrected the dispatch ledger guard semantics and workflow ordering. Root cause: `evaluate_dispatch_ledger()` used `len(matching) > 1`, so one already-emitted identical `{correlation_id, stage, iteration, head_sha}` was allowed, and `len(same_correlation) > dispatch_cap`, so exactly-at-cap history could dispatch one more. Fix: normalized ledger entries now carry `status` (`staged` for candidate entries embedded in stage state bundles, `emitted` by default/after guard pass); the guard blocks any emitted logical-key match with `dispatch_duplicate`, blocks when emitted same-correlation count is already `>= dispatch_per_correlation_cap` with canonical `max_iterations`, and ignores a single self-staged candidate to avoid false positives. Workflow fix: review/design/fix-push state bundles append staged candidates only; final dispatch guard reads that bundle before token mint/curl, then after a pass copies final state into the dispatch artifact, appends an emitted ledger entry, rewrites the repository_dispatch payload `state_artifact_name` to `codex-loop-dispatch-<corr>-<iter>`, and only then mints the dispatch App token/calls `/dispatches`. Evidence refreshed in `.omo/evidence/task-23-ledger.txt` and `.omo/evidence/task-23-concurrency.txt`: py_compile exit 0, focused ledger/duplicate/oscillation/max_iterations 36 passed, full relocated suite 350 passed, actionlint exit 0, focused workflow dispatch/concurrency/fix_push 14 passed, git diff check exit 0.

- 2026-06-04: Task 23 workflow-output fix normalized dispatch guard failures into reusable workflow outputs instead of representing them only as a failed guard step. Root cause: `finalize-stage.outputs` still pointed at `steps.finalize.outputs.*`, so a `guard-dispatch` verdict such as `dispatch_duplicate`, `max_iterations`, or `oscillation_detected` could fail the job after initial outputs were already `should_redispatch=true`/`terminal_reason=""`. Fix: `finalize-stage.outputs` now point at `steps.effective_outputs.outputs.*`; `dispatch_guard` writes `guard_ok` and `guard_terminal_reason` and exits 0 on guard failure; `effective_outputs` runs under `always()`, maps guard failures to `next_stage=""`, `lgtm=false`, `should_redispatch=false`, and canonical `terminal_reason=${GUARD_REASON}`; App-token mint, live curl, and dispatch artifact upload are gated on `steps.dispatch_guard.outputs.guard_ok == 'true'`. Successful guard behavior is preserved: the guard still appends the emitted ledger into the dispatch artifact, rewrites payload `state_artifact_name` to `codex-loop-dispatch-<corr>-<iter>`, then token/curl run. Evidence refreshed: `.omo/evidence/task-23-ledger.txt` (py_compile exit 0, focused selector 51 passed, full relocated suite 351 passed) and `.omo/evidence/task-23-concurrency.txt` (actionlint exit 0, workflow focused 15 passed, diff check exit 0).

- 2026-06-04: Task 23 workflow-output fix evidence correction: the refreshed focused selector in `.omo/evidence/task-23-ledger.txt` is `49 passed, 302 deselected`, not 51 passed. Full relocated suite remains `351 passed`; actionlint, workflow-focused tests, and diff check remain exit 0.

- 2026-06-04: Task 24 hardened the HSI fork/trust boundary with explicit defense-in-depth gates on the secret/OIDC/write-capable paths in `.github/workflows/codex-loop-reusable.yml`. The live `trust-and-stale-guard` (gh pr view: state/headRefOid/baseRefName/headRepository/author) already classified `pr_closed`/`stale_head`/`base_ref_mismatch`/`fork_pr`(live head_repo_full_name != REPOSITORY)/`untrusted_repository_owner`/`untrusted_requester`(requested_by cross-checked vs live author_login, org owner, or *[bot]) and `setup-relay` + every stage entrypoint already gated on `trusted == 'true'`. Task 24 ADDED: (1) `fix-push` if now also requires `needs.trust-and-stale-guard.outputs.fork == 'false'` (was trusted-only; trusted already implies non-fork, so this is pure defense-in-depth); (2) all 8 finalize-stage dispatch steps (checkout/install/build-payload/download-final-state/guard = 5 initial_guard steps; App-token mint + repository_dispatch curl + upload = 3 live_guard steps) now also require `trusted == 'true' && fork == 'false'` in their `if`, so a fork/untrusted PR cannot reach the OIDC relay token, App push token, App dispatch token, repository_dispatch, or git push even if the `should_redispatch=false` derivation regressed. No Python source changed: the push-side `validate_push_target` (rejects fork via same_repo/head!=base) and `provenance.validate_pull_request_target_safety` already exist and are unit-tested. Evidence: `.omo/evidence/task-24-fork.txt`, `.omo/evidence/task-24-samerepo.txt`. Verified: actionlint exit 0 on all 3 workflows; focused `-k "fork or untrusted"` 6 passed; `-k "same_repo or trusted"` 15 passed; full relocated suite 351->358 passed; rendered-if simulation proved fix-push + dispatch secret/write steps run only for trusted same-repo and are blocked for fork and untrusted-requester scenarios; git diff --check exit 0; generated python artifacts cleaned (none in git status).
- 2026-06-04: Task 24 test additions in `setup/codex-review/tests/workflow/test_orchestrator_shape.py` (only file edited besides the workflow): updated `test_continuation_dispatch_is_guarded_off_in_dry_run_and_terminal_paths` initial_guard/live_guard expected strings to include the new `trusted == 'true' && fork == 'false'` gate (strengthening, not weakening), and added 7 tests: `test_fix_push_requires_trusted_non_fork_same_repo`, `test_dispatch_secret_and_write_steps_require_trusted_and_non_fork` (asserts all 8 should_redispatch-gated steps carry trusted+fork and includes the 2 secret/write steps), `test_setup_relay_is_gated_on_trusted_before_oidc_and_secrets`, `test_trust_guard_classifies_fork_untrusted_owner_and_requester_from_live_pr_state` (proves requested_by is cross-validated against live author_login, not payload-only), `test_stage_entrypoints_directly_require_trusted_guard`, `test_downstream_stage_jobs_depend_on_trusted_gated_upstream` (transitive protection via setup-relay/entrypoint needs), and `test_untrusted_fork_pr_cannot_reach_relay_secrets_or_dispatch_paths`. Preserved Task 20/21/22/23 semantics: two-phase push, verified updated_head_sha, App-token dispatch, dispatch ledger/caps gates all unchanged. Tests named to be selected by both `-k "fork or untrusted"` and `-k "same_repo or trusted"`.

- 2026-06-04: Task 25 made every non-LGTM terminal observable label/comment/issue-free and stopped failure masking, editing only `.github/workflows/codex-loop-reusable.yml` + a new test file `setup/codex-review/tests/workflow/test_terminal_visibility.py` + a docs subsection. Added three `finalize-stage` steps AFTER `effective_outputs` (Task 23 source of truth), all `if: always()`: (1) `terminal_state` builds machine-readable `codex-review-artifacts/terminal/codex-loop-state.json` via `jq -n` (keys: schema_version=`codex-loop-state.v1`, stage, pr_number, head_sha, base_ref, correlation_id, iteration, terminal_reason, lgtm, should_redispatch, next_stage, state_run_id, state_artifact_name, updated_head_sha, dry_run, selected_result, plus outcome/disposition/manual_action/terminal_artifact_name/state_owner_run_id) and human `terminal-summary.md` (terminal reason, next manual action, selected state artifact + `gh run download` lookup hint, explicit "No labels, comments, or issues are used for loop state" note), then `cat`s the summary into `$GITHUB_STEP_SUMMARY`; (2) upload-artifact `codex-loop-terminal-<corr>-<iter>` path `codex-review-artifacts/terminal` retention 7; (3) `terminal_conclusion` exits non-zero for failure terminals. To expose `selected_result`, added one `echo "selected_result=${selected_result}"` to the existing `finalize` step's GITHUB_OUTPUT (step output only; reusable workflow_call job outputs unchanged, so the FINALIZE_OUTPUT_KEYS contract test still holds). Evidence: `.omo/evidence/task-25-visibility.txt`, `.omo/evidence/task-25-no-mask.txt`.
- 2026-06-04: Task 25 conclusion partition (single source = `steps.effective_outputs.outputs.terminal_reason`): NON-FAILURE (run stays green) = empty reason (LGTM terminal OR in-progress continuation) | lgtm | dry_run | no_fix_needed | no_fix_changes | empty_patch | issue_created; FAILURE (exit 1 -> workflow conclusion=failure) = every other canonical reason. Verified the partition equals the full `schemas/terminal-reason.v1.json` enum minus the 6 non-failure literals, so no reason is silently unclassified. Continuation (should_redispatch=true) has empty reason -> green by design, and a failed dispatch `curl -fsS` fails its own step (the job goes red) so it is not masked even though terminal_conclusion sees empty reason. GitHub Actions has no easy job-level "neutral" conclusion, so neutral was not used: failure terminals are exit 1, justified in the workflow comment + docs.
- 2026-06-04: Task 25 verification: `/opt/homebrew/bin/actionlint` (1.7.12) clean on all three codex-loop workflows; focused `-k "terminal or visibility or summary or conclusion or no_mask"` 17 passed/351 deselected; new `test_terminal_visibility.py` 10 passed; full relocated suite `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q` 368 passed; `git diff --check` clean; no generated python artifacts (`__pycache__`/`*.pyc`/`*.pyo`/`.egg-info`) left tracked/untracked. Manual QA: extracted the verbatim `terminal_state` + `terminal_conclusion` run scripts from the YAML and executed them under simulated env for 8 scenarios (lgtm/dry_run/continuation/no_fix_needed = exit 0 green; fork_pr/stale_head/dispatch_duplicate/max_iterations = exit 1 failure); every state JSON carried all required keys and the summary carried the no-labels note + next action + lookup hint. Did NOT implement live/dry-run dispatch smoke (Task 26 owns runtime evidence).

- 2026-06-04: Task 26 dry-run `repository_dispatch` smoke is BLOCKED (NO event sent, neither dry_run=true nor false). Root cause (who/what/when/why/how): the full Task 5-25 pipeline is UNCOMMITTED in the HSI working tree on branch `work/codex-loop-reusable-workflow` (` M` codex-loop-dispatch.yml + codex-loop-reusable.yml, `??` setup/); only the PR #18 SCAFFOLD is on the default branch `main`. Read-only `gh` confirms: default branch=main; open PRs=`[]` (no same-repo dry-run test PR); prior codex-loop-dispatch runs=`[]` (no run ever produced a state artifact); main codex-loop-dispatch.yml blob sha `176dd72b2db7df0dad2c5628dc91a456a3e766ee` with `grep -c state_run_id`=`0` (pre-Task-10 scaffold, does NOT require/validate state pointers), main reusable sha `a8070dc36e06cdf6422a63c4e01beb00238206d2`; working-tree dispatch `git hash-object`=`d3abd72c4328058a085c7d7b939cec51bdc2fd01` (differs). `repository_dispatch` only fires workflows already on the default branch (docs:314), so a dispatch now would exercise the scaffold (not the code under test) = misleading; and the MUST-DO `state_run_id`/`state_artifact_name` pointing at a real artifact cannot be satisfied. Per AGENTS.md + Task 26 MUST-NOT, recorded the blocker + unblock handoff (merge pipeline to default branch -> open same-repo non-fork dry-run test PR -> seed first state artifact via one reusable run -> dispatch dry_run=true only) in `.omo/evidence/task-26-smoke-blocked.txt`, and substituted deterministic negative evidence. No `task-26-smoke.json` was created because no run occurred.
- 2026-06-04: Task 26 negative runtime evidence is GREEN and proves every negative path ends in a canonical, VISIBLE terminal (no silent loop). Command: `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q -k "stale or max_iteration or max_iterations or fork or pr_closed or missing_app or relay or dispatch or terminal"` => `59 passed, 309 deselected`; full relocated suite `uvx --with-editable setup/codex-review pytest setup/codex-review/tests -q` => `368 passed`. Mapped each path to its canonical terminal + covering test(s): stale_head (test_commit_push_routes_live_head_drift_to_stale_head, test_fix_push_routes_stale_and_no_fix_changes_canonically), max_iterations (test_dispatch_guard_uses_iteration_greater_equal_max_iterations, _enforces_per_correlation_cap_as_max_iterations), fork_pr/untrusted (test_push_target_rejects_fork_pr, test_fork_pr_is_blocked_in_trust_guard, test_fix_push_requires_trusted_non_fork_same_repo, test_untrusted_fork_pr_cannot_reach_relay_secrets_or_dispatch_paths, test_trust_guard_classifies_fork_untrusted_owner_and_requester_from_live_pr_state), dispatch_duplicate (test_dispatch_guard_blocks_duplicate/_blocks_one_existing_identical_entry_before_live_dispatch), oscillation_detected (test_dispatch_guard_detects_repeated_stage_head_signature_as_oscillation), relay (test_oidc_relay_token.py x5 + test_setup_relay_mints_relay_token_locally_via_oidc). KEY: `pr_closed` and `missing_app_credentials`/`app_token_scope_invalid` do NOT appear in any test FUNCTION name but ARE covered: `pr_closed` is classified in codex-loop-reusable.yml:254 and `test_terminal_conclusion_partition_covers_full_canonical_enum` loads schemas/terminal-reason.v1.json and asserts FAILURE_partition == canonical_enum - NON_FAILURE_REASONS where NON_FAILURE_REASONS={lgtm,dry_run,no_fix_needed,no_fix_changes,empty_patch,issue_created}; thus pr_closed + missing_app_credentials + app_token_scope_invalid (in REQUIRED_FAILURE_REASONS) all fall into the exit-1/conclusion=failure branch with terminal artifact + summary + step-summary. Evidence: `.omo/evidence/task-26-negative.txt`.
- 2026-06-04: Task 26 was READ-ONLY w.r.t. HSI: no workflow/source/test/doc/schema edited (so actionlint was not required this task), Tasks 20-25 semantics untouched. Generated python artifacts (`__pycache__`/`*.pyc`/`*.pyo`/`.egg-info`) created by the pytest runs were removed from disk; HSI `git status --short --untracked-files=all` shows none and `git diff --check` rc=0. The only deliverables are RS evidence files (`task-26-smoke-blocked.txt`, `task-26-negative.txt`) + these notepad entries. LSP diagnostics remain unavailable locally (yaml-language-server / basedpyright-langserver not installed), as recorded since Task 17.

- 2026-06-04: Task 27 added the thin SHA-pinned consumer entry adapter `rs-builder-relayer-client/.github/workflows/codex-loop-review-adapter.yml` (only file created; no RS source/legacy-workflow edits). It triggers on `pull_request_target` `types: [opened, synchronize, reopened, ready_for_review]`, top-level `permissions: {}`, top-level `concurrency.group: codex-loop-review-adapter-${pr_number}-${head_sha}` `cancel-in-progress: true` (deliberately DISTINCT from the core's `codex-loop-${correlation_id}` group so caller/callee never cancel each other). A single reusable-calling job `codex-loop-review` gates drafts with `if: ${{ github.event.pull_request.draft == false }}` (so `ready_for_review` re-enters once draft clears), declares the minimal ceiling `contents: write` + `pull-requests: write` + `id-token: write` (NO issues/labels), and `uses: DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@727f713d2fc497b735ac17b575b74e282b09830e` (exact 40-hex SHA from Task 1, NOT a branch/tag) with `secrets: inherit`. Inputs forward the live PR context: `pr_number`/`head_sha`/`base_ref` from `github.event.pull_request.*`, `requested_by` from `github.event.sender.login`, literal `stage: review`, `iteration: 0`, `dry_run: true`, and deterministic `correlation_id: codex-loop-pr-${pr_number}-${head_sha}` (stable per (PR, head); a synchronize's new head SHA yields a new id, preserving uniqueness across synchronize events). `max_iterations` is intentionally omitted to inherit the reusable default (5). No `workflow_dispatch`/`repository_dispatch` (Task 28 owns those); no live path (`dry_run` is never false).
- 2026-06-04: Task 27 KEY finding: the pinned reusable interface had to be read from GitHub via `gh api ...?ref=727f713...` because the local HSI clone does NOT contain the merge commit `727f713d...` (clone is on `work/codex-loop-reusable-workflow`; `git cat-file -t 727f713...` => "could not get object info", so `git show 727f713...:path` fails). The pinned SCAFFOLD reusable (`workflow_call`) requires exactly 7 inputs (`pr_number:number`, `head_sha:string`, `base_ref:string`, `stage:string`, `iteration:number`, `correlation_id:string`, `requested_by:string`) plus optional `max_iterations:number=5` and `dry_run:boolean=true`, and declares only optional secrets `CODEX_GITHUB_APP_ID`/`CODEX_GITHUB_APP_PRIVATE_KEY` (unused under dry_run). The HSI dispatch + manual scaffold adapters at the same SHA both call `uses: ./.github/workflows/codex-loop-reusable.yml` with job-level ceiling `contents/pull-requests/id-token: write` + `secrets: inherit` and top-level `permissions: {}`; the RS adapter mirrors that caller shape but with the cross-repo SHA-pinned `uses:`. Because each `with:` value is a single `${{ }}` expression (or YAML literal), GitHub preserves native types so `pr_number`/`iteration`/`dry_run` reach the number/boolean inputs without `fromJSON` wrapping (the dispatch adapter needed `fromJSON` only because step outputs are always strings).
- 2026-06-04: Task 27 verification (all green): `/opt/homebrew/bin/actionlint .github/workflows/codex-loop-review-adapter.yml` exit 0 (clean); `actionlint .github/workflows/*.yml` reports ONLY a PRE-EXISTING `SC2129` shellcheck STYLE note in legacy `codex-issue.yml:52` (that file is unchanged per `git status`, left untouched to avoid scope creep). SHA-pin grep matches the exact 40-hex pin; moving-ref grep `@(main|work/|v[0-9]|master)` => no match; `@[^0-9a-f]` => no match. `dry_run: true` present, `dry_run: false` absent; draft-exclusion present; `workflow_dispatch|repository_dispatch` absent. `python3 yaml` parse asserts the full contract (CONTRACT OK). `git diff --check` exit 0; `git status --short` shows only `?? .github/workflows/codex-loop-review-adapter.yml` + `?? .omo/`; no generated python artifacts. Read-only cross-check: `gh api .../commits/main -q .sha` => `727f713d2fc497b735ac17b575b74e282b09830e` (pin == HSI main HEAD) and the reusable blob exists at that ref (`a8070dc3...`). Evidence: `.omo/evidence/task-27-adapter.txt`, `.omo/evidence/task-27-no-branch-pin.txt`.
- 2026-06-04: Task 27 OBSERVATION/handoff for Task 28-29: (1) cross-repo reusable-workflow calls within DongwonTTuna-Labs require the org/repo Actions setting "Accept actions and reusable workflows from selected/other repos" to permit RS -> HSI; this is an operational precondition outside the workflow file and was NOT verifiable from the local clone. (2) The adapter cannot actually run/redispatch until the full Task 5-25 pipeline is merged to HSI `main` (Task 26 blocker); pinning to `727f713...` currently targets the PR #18 SCAFFOLD reusable, which is the only consumer-pinnable merged scaffold per the Task 26/27 dependency note. A real dry-run smoke remains sequenced into the live-capable wave, not Task 27.
- 2026-06-04: Task 27 VERIFICATION-FIX (orchestrator full-gate failure): the required gate is `actionlint .github/workflows/*.yml` (full glob), not adapter-only. The earlier turn's note that the pre-existing `SC2129` in legacy `codex-issue.yml:52` could be "left untouched" was WRONG for the acceptance criteria, since the full glob must be exit 0. Applied the narrowest possible fix: grouped the four CONSECUTIVE `$GITHUB_OUTPUT` writes in `codex-issue.yml` (the `proceed=true`/`review_run_id`/`design_run_id`/`fix_run_id` block right after `find_run()`) into one `{ echo ...; ... } >> "$GITHUB_OUTPUT"` block, keys/values verbatim and `find_run` still executed via command substitution inside the group. The unrelated `echo "proceed=false" >> "$GITHUB_OUTPUT"` inside the `case` branch was NOT consecutive so SC2129 never flagged it and it was left alone. `git diff` confirms the change is exactly that 4-line->6-line block, nothing else. Now `actionlint .github/workflows/codex-loop-review-adapter.yml` exit 0 AND `actionlint .github/workflows/*.yml` exit 0; adapter SHA pin/dry_run/draft/secrets/permissions all unchanged; `dry_run: false` absent; `git diff --check` clean; `git status --short` = ` M codex-issue.yml` + `?? codex-loop-review-adapter.yml` + `?? .omo/`; no generated python artifacts. Refreshed `.omo/evidence/task-27-adapter.txt` + `.omo/evidence/task-27-no-branch-pin.txt` with the passing full-gate raw output. LESSON: a "pre-existing" lint finding in an UNMODIFIED file still fails a glob-scoped gate; when the acceptance command lints `*.yml`, treat any non-zero exit as in-scope and apply the minimum compatibility fix rather than deferring.

- 2026-06-04: Task 28 added the two remaining thin SHA-pinned consumer entry adapters to `rs-builder-relayer-client/.github/workflows/`: `codex-loop-manual-adapter.yml` (`on: workflow_dispatch`) and `codex-loop-dispatch-adapter.yml` (`on: repository_dispatch: types: [codex-loop]`). Both keep top-level `permissions: {}`, a SINGLE reusable-calling job with the minimal ceiling `contents: write`/`pull-requests: write`/`id-token: write` (NO issues/labels), `uses: DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@727f713d2fc497b735ac17b575b74e282b09830e` (same exact 40-hex Task 1/Task 27 pin, no branch/tag/moving ref), and `secrets: inherit`. Manual adapter exposes typed inputs `pr_number:number`, `head_sha:string`, `base_ref:string`, `stage:choice default review (options review/design/fix/issue)`, `iteration:number default 0`, `correlation_id:string`, `requested_by:string`, `max_iterations:number default 5`, `dry_run:boolean default true`, forwarded via `${{ inputs.* }}` (typed-input context preserves native number/boolean types into the reusable, no fromJSON needed). Dispatch adapter maps `${{ github.event.client_payload.* }}` straight into the same 9 inputs, never mutating the payload, with `concurrency.group: codex-loop-dispatch-adapter-${client_payload.correlation_id}` `cancel-in-progress: false` (keyed on correlation_id per the Task 5 evolved design, distinct prefix from the core's `codex-loop-${correlation_id}` so caller/callee never cancel, false to preserve in-flight loop iteration work). No manual or adapter path sets `dry_run: false`; the only live path requires an operator/emitter to explicitly opt out plus the core's App secrets + trust guards.
- 2026-06-04: Task 28 KEY: re-fetched the pinned reusable contract via `gh api '.../contents/.github/workflows/codex-loop-reusable.yml?ref=727f713d...' -H 'Accept: application/vnd.github.raw'` (558 lines, the PR #18 SCAFFOLD) because the local HSI working tree reusable is 2823 lines (Task 5-25 pipeline, uncommitted on `work/codex-loop-reusable-workflow`, NOT the pinned merge commit). The pinned scaffold `workflow_call` accepts ONLY 9 inputs (7 required: pr_number/head_sha/base_ref/stage/iteration/correlation_id/requested_by; 2 optional: max_iterations=5, dry_run=true) and has `grep -c state_run_id`=0, confirming the Task 1/10/26 warning. Therefore BOTH adapters intentionally OMIT `state_run_id`/`state_artifact_name` to stay runtime-compatible with the pin; comments + evidence document this scaffold limitation and instruct not to forward those pointers until the consumer re-pins to a reusable SHA that accepts them. The pinned scaffold also does NOT emit any continuation `repository_dispatch` (that is a later HSI commit), so the dispatch adapter is forward-looking infra: with the current pin nothing auto-POSTs codex-loop events; it must additionally be merged to the consumer DEFAULT branch before repository_dispatch can fire it at all (documented as a `!!! DEFAULT-BRANCH REQUIREMENT !!!` comment block).
- 2026-06-04: Task 28 dispatch `dry_run` safe default uses `${{ github.event.client_payload.dry_run != false }}`: GitHub `!=` numeric coercion gives true(bool)->true, false(bool)->false (honors an explicit trusted live request), null/absent->true (SAFE DEFAULT), and string "false"/"true"->true (a string cannot enable live). This satisfies "use payload dry_run only if present, else default true" within a single reusable-calling job (a job with `uses:` cannot also run shell steps, so the Task 22-style validate-then-fromJSON two-job pattern was deliberately NOT used here per the Task 28 one-job directive; the reusable core's own `validate` job still rejects malformed inputs).
- 2026-06-04: Task 28 verification (all green): `/opt/homebrew/bin/actionlint` 1.7.12 -> `actionlint .github/workflows/*.yml` exit 0 (full glob gate; Task 27 `codex-issue.yml` SC2129 grouped-output fix preserved and still passing), and exit 0 on each new adapter individually. Trigger greps: manual `workflow_dispatch:` present, dispatch `repository_dispatch:` + `types: [codex-loop]` present. SHA: exact 40-hex pin present in both `uses:`; `@[^0-9a-f]` no match; `@(main|work/|v[0-9]|master)` no match. `dry_run:\s*false` no match anywhere (reworded the manual comment that referenced the literal so the safety grep stays clean). `secrets: inherit` on both calling jobs. `python3 yaml` parse asserts: one job each, top-level `permissions: {}`, job ceiling has no issues perm, `uses == pin`, `with` keys == the 9 pinned inputs, state pointers absent, `secrets: inherit` (CONTRACT OK for both). `git diff --check` exit 0; `git status --short` = ` M codex-issue.yml` + `?? codex-loop-dispatch-adapter.yml` + `?? codex-loop-manual-adapter.yml` + `?? codex-loop-review-adapter.yml` + `?? .omo/`; generated-artifact scan empty. Evidence: `.omo/evidence/task-28-adapters.txt`, `.omo/evidence/task-28-manual-dryrun.txt`. Did NOT run a live repository_dispatch (Task 28 MUST-NOT + Task 26 default-branch/state-artifact preconditions still unmet); actionlint + YAML parse + contract greps are the static verification gates for these remote-pinned reusable-caller workflows. LSP/YAML language servers remain unavailable locally as recorded since Task 17.

- 2026-06-04: Task 29 disabled the four RS legacy label workflows without deleting them by replacing `pull_request_target: types: [labeled]` with manual-only `workflow_dispatch` stubs and explicit "Disabled during Codex loop migration" comments. `grep` now proves no legacy `types: [labeled]` and no legacy `pull_request_target:` remain, while the new adapters still expose the only automated entry points in the working tree (`codex-loop-review-adapter` PR events, `codex-loop-dispatch-adapter` repository_dispatch, plus manual debug). Residual `github.event.label.name` guards remain as dead preserved legacy code under manual-only triggers for Task 30 removal. Evidence: `.omo/evidence/task-29-no-overlap.txt`.
- 2026-06-04: Task 29 dry-run/live parity was honestly blocked and no dispatch was sent. Read-only `gh workflow list` on RS default branch shows only the legacy workflow file names, not the new Task 27/28 adapters; the RS adapters are still working-tree/unmerged from the default branch perspective. The pinned HSI SHA `727f713d2fc497b735ac17b575b74e282b09830e` is the PR #18 scaffold (`ci(actions): add reusable Codex loop workflows`), not the Task 5-25 live-capable continuation pipeline, and no explicit approved Task 29 live PR plus prior dry-run smoke success was available. Evidence/blocker: `.omo/evidence/task-29-live-blocked.txt`.
