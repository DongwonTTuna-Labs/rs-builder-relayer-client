# Issues

- 2026-06-04: PR #18 was initially observed as `OPEN` with mergeCommit `null`, so Task 1 was briefly marked blocked. In the same continuation turn it was subsequently observed as `MERGED`; the Task 1 checkbox was reset to unchecked and the task is being re-run to capture the 40-char main SHA. URL: https://github.com/DongwonTTuna-Labs/home-server-infra/pull/18

- 2026-06-04: RESOLVED: The previous PR #18 merge gate blocker is resolved. Re-check returned `MERGED`, main SHA `727f713d2fc497b735ac17b575b74e282b09830e`, and both required workflow files exist on `main`.

- 2026-06-04: RESOLVED (Task 6): HSI `tests/workflows/test_workflow_contracts.py` was RED at baseline (2 failures) before Task 6 touched it. Root cause: Task 5 changed the reusable iteration guard to `INPUT_ITERATION >= INPUT_MAX_ITERATIONS` and unified dispatch concurrency to `codex-loop-${{ github.event.client_payload.correlation_id }}`, but the PR#18 assertions still expected the old `INPUT_ITERATION > INPUT_MAX_ITERATIONS` and the old `pr_number}-...{head_sha` concurrency group. Task 5 edited workflows without updating these two stale assertions. Task 6 realigned both to the current Task 5 contract (the `>=` change is stricter, not weaker; the concurrency change asserts the canonical `correlation_id` identity already used by the reusable core). This is a correction of stale assertions, not a weakening, and was required to meet the `uvx pytest tests/workflows -q` green gate. who=Task 5 workflow edits / what=assertions left checking removed strings / when=baseline of Task 6 / why=tests not updated alongside workflow change / how=realign 2 assertions to current contract. Verified 19->42 green.

- 2026-06-04: OPEN (Task 7 verification): exact command `uvx pytest setup/codex-review/tests -q` is not runnable as a green gate immediately after relocation. Root cause A: `uvx pytest` creates an isolated pytest environment and does not install the relocated package dependencies from `setup/codex-review/pyproject.toml`, so collection fails with `ModuleNotFoundError: No module named 'yaml'` and `No module named 'cryptography'`. Root cause B after supplying dependencies with `uvx --with PyYAML --with cryptography --with jsonschema pytest setup/codex-review/tests -q`: 213 tests pass and 35 copied workflow-contract tests fail because they still assert the old RS split label workflow files (`.github/workflows/codex-review.yml`, `codex-design.yml`, `codex-fix.yml`, `codex-issue.yml`) and `.github/actions/setup-codex-review/action.yml`, while HSI currently has the reusable/dispatch/manual workflow shape from Tasks 1-6. This task preserved the tests and did not add legacy workflows or delete assertions because Task 7 scope is pure package relocation plus minimal trusted install hook; Task 11 is the planned green-test realignment point. who=Task 7 pytest gate / what=collection dependency miss plus old split-workflow assertions / when=HSI relocated package verification / why=uvx isolated deps and copied RS tests target pre-migration workflow topology / how=install/help/import/schema passed, dependency-supplied pytest isolated remaining topology failures.

- 2026-06-04: OPEN (Task 7 local tooling): `uvx actionlint` is unavailable locally because `actionlint` is not in the uv package registry (`No solution found when resolving tool dependencies`). Workflow syntax was still checked indirectly by preserving narrow YAML edits and `git diff --check`; run a proper `actionlint` binary in CI or an environment with actionlint installed before PR merge. who=local uvx tool resolver / what=actionlint binary unavailable / when=HSI workflow install-hook verification / why=uvx cannot install actionlint from PyPI / how=`uvx actionlint` failed before lint execution.

- 2026-06-04: RESOLVED (Task 8): genericizing HSI `setup/codex-review/config.yml` removed `trusted.codex_review_authors` (the RS bots), which `tests/unit/test_resolved_finding_filter.py::test_cli_collect_resolved_and_filter_roundtrip` relied on to recognize a thread authored by `codex-reviewer-for-dongwonttuna` as a trusted codex author. Root cause: that test sourced its trusted-author policy from the (formerly RS-specific) base `config.yml` rather than from a consumer override. Fix: realigned the test to pass the relocated RS author config via the new `--config-override tests/fixtures/config/consumer_override.yml`; assertions (`mem["count"]==1`, `suppressed_resolved_count==1`) are unchanged, so this is a correction to match the generic-core + consumer-override design, not a weakening - it now also exercises the override mechanism end-to-end through the CLI. who=Task 8 config genericization / what=base config.yml no longer carries RS trusted authors / when=Task 8 verification / why=trusted authors are repo-specific and moved to the consumer override layer / how=test now injects them via --config-override; verified 24/24 in the two focused files and 217/217 unit.

- 2026-06-04: OPEN (Task 8, inherited from Task 7): the full `pytest setup/codex-review/tests -q` is still not a green gate. After Task 8, `tests/unit` is 217/217 green, but the full suite is 35 failed / 226 passed; all 35 failures are in `tests/workflow/` (`test_orchestrator_shape.py`, `test_permissions.py`) - the legacy RS split-workflow contract tests Task 7 documented (exactly 35). They read `.github/workflows/*.yml` and never load `config.yml`, so Task 8 neither caused nor fixed them. who=legacy copied workflow tests / what=assert pre-migration split workflow + setup-action shape / when=Task 8 full-suite verification / why=HSI now uses the reusable/dispatch topology from Tasks 1-6 / how=Task 11 is the planned green-test realignment point; Task 8 ran the narrow config unit tests directly: `PYTHONPATH=src python3 -m pytest tests/unit -q`.
- 2026-06-04: RESOLVED (Task 9 smoke rerun): first artifact loop-state smoke command failed only in the final print step with `KeyError: 'tmpdir'` because the shell variable holding the temp directory was not exported into the Python subprocess environment. The CLI `loop read-state` and `push record-push` commands had already completed, and rerunning the same smoke with exported `TMPDIR_LOOP_STATE` printed the expected READ/NEXT JSON. who=verification shell command / what=temp path unavailable to Python print subprocess / when=Task 9 smoke verification / why=shell-local variable was referenced through `os.environ` / how=exported temp path and reran; evidence captured in `.omo/evidence/task-9-state.txt`.

- 2026-06-04: RESOLVED (Task 10): Previous HSI reusable/dispatch workflow boundary did not yet carry Task 2 payload v2 state pointers into the called core, so cross-run state could only be local/default and would have tempted ambiguous future discovery by head SHA/latest run. Root cause: Task 9 moved loop memory into JSON artifacts but stopped at local file read/write helpers; the workflow transport contract had not been wired. Fix: require `state_run_id` and `state_artifact_name` at dispatch/manual/reusable inputs, validate both before relay/stage execution, download the exact artifact by explicit run-id/name, and add focused tests/grep gates proving no active `headSha` discovery. who=Task 9 workflow gap / what=state artifact pointer not transported through workflow boundary / when=Task 10 baseline / why=artifact persistence was implemented before cross-run acquisition wiring / how=explicit run-id/name validation and download transport.

- 2026-06-04: RESOLVED (Task 11, closes the OPEN Task 7/Task 8 inherited full-suite gap): the relocated `setup/codex-review/tests` full suite is now green (`281 passed`). Root cause was exactly as documented in the Task 7/8 OPEN entries: the RS-copied `tests/workflow/` contract tests asserted the pre-migration split-workflow topology (`codex-review.yml`/`codex-design.yml`/`codex-fix.yml`/`codex-issue.yml` + `setup-codex-review` composite action via `_pipeline.PIPELINE_FILENAMES`), while HSI now ships the reusable/dispatch/manual topology from Tasks 1-10. A second, subtler failure mode: 5 of the `test_orchestrator_shape.py` tests were passing VACUOUSLY at baseline because `_pipeline` helpers returned empty strings/dicts for the non-existent old files; re-pointing `_pipeline` at the real files would have flipped those to real evaluation, so a partial fix was impossible and a comprehensive rewrite of all 5 affected files was required. Fix = test realignment only (no workflow/source change): rewrote `_pipeline.py` + `test_orchestrator_shape.py` + `test_permissions.py` + `test_no_inline_python_or_schema_bloat.py` + `test_no_resolve_checker_workflow.py` to the current contract, preserving/strengthening security intent (permission ceilings, trust boundaries, no-PAT/app-token, no issues:write, state transport, no PR-head execution). who=RS-copied workflow contract tests (Task 7 relocation) / what=assertions targeting removed split-workflow topology + vacuous-pass helpers / when=Task 11 baseline (35 failed, 244 passed) / why=copied tests predate the reusable-core migration / how=re-point `_pipeline` to the 3 live workflows and rewrite assertions; 48->50 test funcs, zero deletions.

- 2026-06-04: DECISION (Task 11, security-assertion adaptation worth flagging at review): the legacy `test_actions_are_pinned_and_checkout_credentials_not_persisted` asserted BOTH (a) GitHub Actions are SHA-pinned (no `@v4`) and (b) every checkout sets `persist-credentials: false`. The current Wave-2 workflows intentionally use moving version tags (`actions/checkout@v4`, `actions/download-artifact@v4`, `actions/upload-artifact@v4`, `openai/codex-action@v1`), and the canonical current contract (HSI root `tests/workflows/test_workflow_contracts.py` from Task 6, which is green) does NOT require Action SHA-pinning. Enforcing (a) would have forced an out-of-scope workflow change and contradicted the established Wave-2 contract. So the realigned `test_checkout_credentials_are_never_persisted` keeps the credential-hardening half (b) strongly (`count(actions/checkout@) == count(persist-credentials: false)`, the trust-boundary-relevant property) and drops the Action SHA-pin half (a) as not part of the current topology contract. Note: AGENTS.md SHA-pin discipline targets production crate/dependency pins and the core CLI SHA source (Task 4), not GHA marketplace action tags. If Wave 3 wants GHA actions SHA-pinned, that belongs in a workflow PR (Task 12+), not in this test-green task. who=Task 11 realignment / what=dropped GHA SHA-pin assertion, kept persist-credentials assertion / when=Task 11 / why=current contract uses version tags by design; pinning would be an out-of-scope workflow behavior change / how=split the legacy two-part assertion, keep the security-relevant half.

- 2026-06-04: RESOLVED (Task 12): The reusable workflow still had the PR#18-era single `run-stage` job, generic `Run Codex loop stage ... for PR` prompt, `dry-run-placeholder`, and `finalize` output normalizer, which would have forced Tasks 13-16 to build rich stages inside one generic prompt path. Root cause: Task 11 intentionally realigned tests to the then-current Wave-2 shape but did not change workflow behavior; Task 12 is the first Wave-3 skeleton change. Fix: split stage entrypoints into explicitly gated `review-stage`/`design-stage`/`fix-stage`/`issue-stage` jobs, move workflow outputs to `finalize-stage`, keep common checkout topology in `setup-relay`, and update tests to assert the new shape/security ceiling. who=Wave-2 reusable workflow skeleton / what=generic stage path still active / when=Task 12 baseline / why=stage fan-out had not yet been introduced / how=per-stage gated skeleton jobs plus finalize-stage normalization and grep evidence proving generic prompt/placeholder absence.

- 2026-06-04: DECISION (Task 13, architecture worth flagging at review): a real GitHub Actions `strategy.matrix` is a job-level construct, so the 5-axis review fan-out cannot live inside the single `review-stage` job from Task 12. Task 13 therefore expanded the review stage into four stage-gated jobs (`review-collect` -> `review-axes` (matrix) -> `review-combine` -> `review-techlead`) and rewired `finalize-stage` to consume `review-techlead` outputs. This necessarily changed the Task 12 exact-job-order contract and the `STAGE_JOBS` iteration sets, so several `test_orchestrator_shape.py`/`test_permissions.py`/`test_no_resolve_checker_workflow.py` assertions were updated honestly (and new 5-axis / dry-run-posting / state-bundle assertions added). One contract was deliberately refined, not weakened: `test_intra_workflow_handoffs_use_job_outputs_not_loose_artifacts` now exempts `review-axes` because a matrix job cannot expose aggregated job outputs and MUST hand per-axis results to combine via scoped, retention-bounded artifacts (mirrors the reference `codex-review.yml`). who=Task 13 review wiring / what=matrix forces multi-job review + Task 12 job-order contract change / when=Task 13 / why=GHA matrix is job-scoped / how=4 gated review jobs + honest test realignment, suite 281->284 green.

- 2026-06-04: OPEN (Task 13, reserved for the live-capable PR; affects Task 19 wiring): the review pipeline is wired and deterministic but NOT live. In HSI `dry_run` (default true) `setup-relay` does not mint the OIDC relay token, so no model can run; the review/axes/techlead jobs use `codex-review … default-result` model-free fallbacks and `techlead publish --dry-run`, and the review jobs are read-only (no `id-token: write`, no app token, no `issues: write`, no `pull-requests: write`) so they CANNOT post. Therefore live per-axis model inference (`openai/codex-action`, still forbidden by `test_stage_skeleton_jobs_do_not_use_generic_model_prompt`) and live review posting are deliberately deferred to a separate live-capable change, which must add: relay-token minting per model job, `review model-result`/`techlead model-result` (prompt + schema) wiring against PR-head data, real `pr-context`/`changed-lines`/`docs-context` production for the loop (the loop has no `pull_request` event, unlike legacy `codex-review.yml`), and a write-capable posting identity. Also note: `review validate` was intentionally left out of the skeleton because it requires `inspection_evidence` paths (e.g. `AGENTS.md`) to exist under `--repo-path`, a runtime dependency the dry-run skeleton must not assume. who=Task 13 scope boundary / what=live model + posting not wired / when=Task 13 / why=dry-run-default + read-only review jobs + no event context in loop / how=deterministic default-result chain now; live wiring is a follow-up live-capable PR.

- 2026-06-04: DECISION (Task 14, mirrors the Task 13 matrix architecture): a GitHub Actions `strategy.matrix` is job-scoped, so the design cluster-analysis fan-out cannot live in one `design-stage` job. Task 14 expanded design into four stage-gated jobs (`design-prepare` -> `design-analyze` (DYNAMIC matrix from prepared clusters) -> `design-plan` -> `design-chief`) and rewired `finalize-stage` to consume `design-chief` outputs. This changed the Task 12/13 exact-job-order and `SKELETON_STAGE_JOBS`/`STAGE_JOBS` iteration sets, so `test_orchestrator_shape.py` (added `DESIGN_STAGE_JOBS`/`DESIGN_OUTPUT_JOB`; `SKELETON_STAGE_JOBS` shrank to `fix-stage`/`issue-stage`; install-count now `1 + len(REVIEW_STAGE_JOBS) + len(DESIGN_STAGE_JOBS)` = 9; design jobs added to relay-context + intra-handoff output sets; `design-analyze` exempted from job-output handoff like `review-axes`; the old `--arg next_stage "fix"` skeleton assertion replaced by the design route `next_stage="fix"`), `test_permissions.py` (`STAGE_JOBS` now lists the four design jobs, all `contents:read`/`pull-requests:read`/`actions:read`, no `id-token: write`, no write), and `test_no_resolve_checker_workflow.py` (only `Fix`/`Issue` remain "Stage Skeleton"; added the four design job display names) were updated honestly. Added four design tests (inventory/cluster/analyze-matrix/plan/chief presence, dynamic matrix fan-out, dry-run posting-disabled + state-bundle, explicit-run-id-without-head-sha). No tests deleted, no skips. Relocated suite 284 -> 288 passed; acceptance files 47 passed; `actionlint` 0 errors; `git diff --check` clean; no generated python artifacts in HSI `git status`. who=Task 14 design wiring / what=GHA matrix forces multi-job design + job-order contract change / when=Task 14 / why=matrix is job-scoped / how=4 gated design jobs + honest test realignment.

- 2026-06-04: OPEN (Task 14, reserved for the live-capable PR; affects Task 15/fix consumption and Task 19): the design pipeline is wired and deterministic but NOT live. In HSI `dry_run` (default true) `setup-relay` does not mint the OIDC relay token, so the design inventory/cluster/analysis/plan/chief models do not run; the jobs use `codex-review design …` model-free fallbacks and `design_chief publish --dry-run`, and all design jobs are read-only (no `id-token: write`, no app token, no `issues:write`/`pull-requests:write`) so they CANNOT post. Because the prior review loop-state is empty in dry-run, design produces an empty plan -> `no_fix_needed` -> `stop_noop` (it never routes to `fix`); the `run_fix_dispatch`->`fix` redispatch path is wired but unreachable until a live review produces real findings. Live wiring (a follow-up live-capable change) must add: relay-token minting per design model job, the `model-inventory`/`model-clusters`/`model-analysis`/`model-plan`/`design_chief model-result` (prompt + schema) wiring against PR-head data, real design context inputs (`pr-context`/`openspec-context`/`docs-context`), and a write-capable posting identity for `design_chief publish` (non-dry-run). The design state-bundle `codex-loop-design-state-<corr>-<iter>` (contains `design-plan.json` + `chief-decision.json`) is the artifact Task 15/fix consumes via the same explicit `state_run_id`/`state_artifact_name` transport. who=Task 14 scope boundary / what=live design model + posting not wired / when=Task 14 / why=dry-run-default + read-only design jobs + empty prior review state in dry-run / how=deterministic fallback chain now; live wiring is a follow-up live-capable PR.

- 2026-06-04: RESOLVED (Task 15): HSI `stage=fix` was still the Task 12 deterministic single-job skeleton (`Fix Stage Skeleton`) after review/design had been expanded into real multi-job matrix pipelines, so it could not consume Task 14 design state or produce `merged-fix`/`validated-fix` artifacts for later push/finalization tasks. Root cause: Task 12 intentionally left fix internals deferred, and Task 14 only produced the design state bundle without wiring a fix consumer. Fix: split fix into `fix-dispatch` -> `fix-run-agents` matrix -> `fix-merge-validate`, use existing trusted CLI deterministic fallbacks, keep all fix jobs read-only/no-id-token/no-write, synthesize only trusted PR context from workflow inputs, and leave commit/push/record-push/repository_dispatch absent for Wave 3. who=Task 12 fix skeleton + Task 14 deferred consumer / what=fix-stage matrix and state bundle missing / when=Task 15 baseline / why=fix internals were explicitly deferred until after design state shape existed / how=three gated fix jobs with explicit artifact transport and no-push validation bundle.

- 2026-06-04: RESOLVED (Task 15 follow-up): Verification found the initial fix pipeline wiring had a runtime-output bug: `fix-dispatch.outputs.agent_matrix` and `has_agent_tasks` pointed at `steps.plan_tasks.outputs.*`, but the `plan_tasks` shell only echoed `has_agent_tasks` into `$GITHUB_STEP_SUMMARY` and never wrote `agent_matrix` to `$GITHUB_OUTPUT`. Root cause: the test asserted the job output mapping but did not inspect the producing shell step, so the contract was structurally present but empty at runtime. Fix: after `fix_dispatch prepare-agents`, compute `agent_matrix=$(jq -c . agent-matrix.json)` and `has_agent_tasks` from `.include | length`, write both to `${GITHUB_OUTPUT}`, and add a workflow test that inspects the `plan_tasks` run body for both output writes. who=Task 15 workflow/test gap / what=missing GITHUB_OUTPUT writes for dynamic matrix outputs / when=orchestrator follow-up verification / why=coverage checked consumers not producer step / how=explicit output writes plus regression assertion.

- 2026-06-04: DECISION (Task 16): issue fallback remains MVP artifact/summary only. Root cause/decision record: who=Task 16 issue-stage wiring / what=legacy `issue-stage` was only a deterministic skeleton and optional real issue creation would require write permissions plus app token / when=Wave-3 issue stage implementation after review/design/fix dry-run stages / why=the accepted plan default explicitly defers live GitHub issue creation and forbids label/comment/issue-state transport / how=use existing trusted `issue_fallback` CLI non-mutating commands (`infer-reason`, `plan`, `compose`) to produce a terminal artifact and `$GITHUB_STEP_SUMMARY`, but do not call `apply`, do not grant `issues: write`, and do not create `issue-created` or `needs-issue` state markers. This is a deliberate no-live-issue boundary, not a missing implementation.

- 2026-06-04: RESOLVED (Task 17): Initial finalize-stage already declared the seven reusable outputs but only wrote four route fields to `$GITHUB_OUTPUT`, so downstream consumers would receive empty `state_run_id`, `state_artifact_name`, and `updated_head_sha` despite structural YAML outputs existing. Root cause: prior tests checked consumer output mappings and string presence but did not inspect the finalizer producer shell body. Fix: finalizer now initializes `state_run_id=${CURRENT_RUN_ID}`, computes deterministic artifact pointers, keeps `updated_head_sha=""`, writes all seven outputs, and `test_orchestrator_shape.py` now asserts workflow_call outputs, finalize job outputs, and the finalizer shell writes. who=Task 12-16 finalizer skeleton / what=missing producer writes for state pointer outputs / when=Task 17 baseline / why=structural output mapping was not enough to prove runtime `$GITHUB_OUTPUT` writes / how=producer-shell assertions plus focused/full pytest gates.

- 2026-06-04: RESOLVED (Task 17 actionlint): Canonicalizing validation reasons made `require_non_empty()` stop using its first `name` argument, and actionlint surfaced ShellCheck SC2034 (`name appears unused`). Fix: remove the unused local variable while preserving the two-argument call shape for readability and validation symmetry. who=Task 17 terminal_reason canonicalization / what=unused shell local in validation helper / when=actionlint verification / why=reason no longer embeds the field name / how=remove local `name`, actionlint then passed.

- 2026-06-04: OPEN (Task 17 local tooling): LSP diagnostics remain unavailable in HSI because `yaml-language-server` and `basedpyright-langserver` are not installed. Used `/opt/homebrew/bin/actionlint`, `python3 -m py_compile`, focused pytest, full relocated pytest, and `git diff --check` as the available verification gates. who=local LSP tooling / what=yaml/python diagnostics unavailable / when=Task 17 verification / why=language servers missing / how=tool reported install commands; no code workaround added.

- 2026-06-04: OPEN (Task 18, PRE-EXISTING, out of scope): the HSI ROOT workflow harness `tests/workflows/test_workflow_contracts.py` (Task 6) is stale relative to the current reusable workflow and fails 1 test at baseline BEFORE Task 18 touched anything: `test_reusable_workflow_contract_has_guards_concurrency_and_ordering` asserts the legacy markers `max-iterations-exceeded` and `trusted-fix-push-not-implemented`, which Tasks 5/12-17 replaced with the canonical `max_iterations` terminal reason and the real per-stage routing. The canonical green gate used by the migration since Task 11 is `setup/codex-review/tests` (296->317 passed after Task 18), not the root harness. Task 18 verified it neither fixed nor worsened this: the failing assertion is about iteration guards (Task 5 territory), unrelated to bundle transport. who=root tests/workflows harness / what=stale legacy-marker assertions / when=Task 18 baseline (1 failed, 41 passed; same failure pre/post) / why=root harness was not realigned when Tasks 5/12-17 migrated the workflow to canonical terminal reasons / how=left untouched (out of Task 18 scope; realignment belongs to whichever task owns the root harness, mirroring the Task 11 relocated-suite realignment). Baseline `uvx pytest tests/workflows -q` recorded for transparency.

- 2026-06-04: RESOLVED (Task 18 follow-up): the first Task 18 pass left a real gap — common `setup-relay` downloaded `inputs.state_artifact_name` WITHOUT `continue-on-error`, so a missing prior STAGE bundle hard-failed setup-relay before design-prepare/fix-dispatch could run verify-bundle, and finalize emitted `stage_failed` instead of `artifact_missing`. who=setup-relay "Download prior loop state artifact" step / what=hard-fail on missing prior stage bundle preempts the stage verify-bundle gate / when=orchestrator verification of Task 18 / why=download lacked continue-on-error and every stage job gates on setup-relay success / how=made ONLY that download non-blocking (continue-on-error: true, explicit run-id+name preserved); read-state already falls back to empty loop-state, so setup-relay now succeeds on a missing bundle and the stage verifier is the single authority that routes artifact_missing. Guard against over-mapping: only the download is non-blocking — relay-token mint, validate, and trust still hard-fail with canonical reasons (stage_failed / missing_app_credentials / validation_failed / fork_pr), confirmed by an 8-case finalize run-body simulation. Regression test added (`test_setup_relay_prior_state_bundle_download_is_non_blocking`). Verified green: actionlint, focused bundle/missing selectors, workflow-shape suite 69, full relocated suite 318, diff/no-pyc clean.



- 2026-06-04: OBSERVATION (Task 19, NOT fixed by design — schema/impl change is forbidden for this parity task): `codex-review design collect-analyses` emits `{schema_version: design-cluster-analysis.v1, analyses, analysis_count}` WITHOUT the `inspection_evidence` field that `schemas/design-cluster-analysis.v1.schema.json` lists as required, so collect-analyses output does NOT validate against its own declared schema_version. The deterministic `design default-analysis` output DOES include `inspection_evidence` and validates cleanly, so the Task 19 parity test asserts design-cluster-analysis.v1 against `default-analysis` and leaves collect-analyses' missing field as a recorded observation. who=relocated `cli/handlers/design.py` collect-analyses branch / what=required `inspection_evidence` omitted from collect-analyses payload / when=Task 19 parity authoring / why=collect-analyses aggregates per-batch analyses without re-emitting inspection evidence / how=documented only; fixing it would require a stage-implementation/schema change which Task 19 MUST NOT do (belongs to a separate behavior PR). LSP diagnostics unavailable locally; verified via `uvx --with-editable setup/codex-review pytest` + `git diff --check` + py artifact find.

- 2026-06-04: RESOLVED (Task 20): live stale-head mismatch during the write phase would have raised `ValidationError` inside `commit-push`, causing the workflow to look like a generic stage failure instead of the canonical terminal `stale_head`. Fix: `commit_and_push_validated_fix()` now catches the immediate live `validate_current_head()` drift check and returns `push-result.v1 status=stale_head, pushed=false, commit_sha=null`; workflow `fix-push` maps that status directly to `terminal_reason=stale_head`. No-change repeat status was also normalized from legacy `no_diff_repeat` to canonical `no_fix_changes` in validation/commit paths and in `write-validation-outputs` compatibility mapping. Verified with focused stale/no_fix/two_phase selectors, workflow tests, full relocated suite, actionlint, diff check, and generated-artifact cleanup.

- 2026-06-04: DECISION (Task 21, manual QA method worth flagging at review): proving the verified-push path end-to-end requires a real `git push` + `git ls-remote`, but the production path also calls App-token identity checks (`assert_installation_token_for_repo`, `validate_current_head` -> GitHub PR-head API) that need live GitHub App creds (forbidden in the default suite). The QA driver therefore stubbed ONLY those two identity checks (Task 20 territory) and ran the actual remote-verification core against a LOCAL bare remote by pointing `GITHUB_SERVER_URL` at a local dir so `_authenticated_url` resolves to it (no https:// substitution). This exercised real `push_commit`/`verify_remote_head_ref` and proved `updated_head_sha == commit_sha == independent bare-repo head`. who=Task 21 manual QA / what=App-token identity checks cannot run without live creds / when=Task 21 verification / why=push/ls-remote must be real but App identity is Task 20 scope + needs secrets / how=stub identity checks only, run push+ls-remote for real against a local bare remote.
- 2026-06-04: OBSERVATION (Task 21, defensive-code decision): finalize-stage failure override branches (validate/trust/setup/selected_result != success) do NOT clear `updated_head_sha` because it is only set in the fix-push success path, which requires validate/trust/setup success AND `selected_result==success`; no real path reaches an override branch with a non-empty head, so adding clears would be dead defensive code (AGENTS.md: no defensive code for impossible states). The verified-head gate lives where it matters: Python (`verified`-gated) + fix-push normalize (`verified == true && non-empty head`).
- 2026-06-04: OPEN (Task 21 local tooling, inherited): LSP diagnostics remain unavailable in HSI (`yaml-language-server`/`basedpyright-langserver` not installed). Used `/opt/homebrew/bin/actionlint`, `python3 -m py_compile`, focused + full `uvx --with-editable` pytest, a real-push manual QA driver, and `git diff --check` as the verification gates. who=local LSP tooling / what=yaml/python diagnostics unavailable / when=Task 21 verification / why=language servers missing / how=actionlint + py_compile + pytest + manual QA; no code workaround added.
- 2026-06-04: OPEN (Task 22 local tooling, inherited): LSP diagnostics are still unavailable in HSI (`yaml-language-server`/`basedpyright-langserver` not installed). Task 22 used `/opt/homebrew/bin/actionlint`, `python3 -m py_compile`, focused continuation pytest, changed-file pytest, full relocated pytest, Python/YAML guard inspection, manual jq payload QA, `git diff --check`, and generated-artifact scans instead. who=local LSP tooling / what=YAML/Python language servers unavailable / when=Task 22 verification / why=local servers are not installed / how=no workaround in code; evidence in `.omo/evidence/task-22-guard.txt`.

- 2026-06-04: OPEN (Task 23 local tooling, inherited): LSP diagnostics remain unavailable in HSI because `basedpyright-langserver` is not installed (and prior tasks recorded missing YAML LSP as well). Task 23 used `python3 -m py_compile`, `/opt/homebrew/bin/actionlint`, focused/full `uvx --with-editable setup/codex-review pytest`, `git diff --check`, workflow/Python inspection, and generated-artifact scans instead. who=local LSP tooling / what=Python/YAML language server diagnostics unavailable / when=Task 23 verification / why=language servers missing / how=no code workaround; command evidence in `.omo/evidence/task-23-ledger.txt` and `.omo/evidence/task-23-concurrency.txt`.

- 2026-06-04: RESOLVED (Task 23 verification failure): orchestrator review found two real guard bugs. who=Task 23 dispatch ledger implementation / what=duplicate guard required two matching entries and cap guard allowed exactly cap existing entries to dispatch again / when=post-Task-23 verification / why=the first-pass implementation treated a self-staged candidate as the only expected match and encoded a permissive test (`test_dispatch_guard_allows_single_current_ledger_entry`) instead of distinguishing staged vs emitted state / how=added ledger `status`, changed duplicate to any emitted logical-key match, changed cap to emitted count `>= cap`, replaced permissive test with one-emitted-entry duplicate failure plus self-staged allow case, and moved emitted append/payload pointer rewrite to after final guard pass but before live dispatch. Verified in refreshed Task 23 evidence files.

- 2026-06-04: RESOLVED (Task 23 workflow-output verification failure): guard failures were canonical in `dispatch-guard.json` but not in workflow outputs. who=HSI `finalize-stage` output wiring / what=job outputs referenced stale `steps.finalize` values while `dispatch_guard` failed the job / when=post duplicate/cap fix verification / why=no post-guard effective-output normalization existed / how=made guard non-failing with outputs, added `effective_outputs` as the job-output source, and gated App-token/curl/upload on `guard_ok == true`. Verified in refreshed Task 23 evidence files.

- 2026-06-04: OBSERVATION (Task 24, requested_by *[bot] allowance, NOT changed by design): the trust guard's `untrusted_requester` check allows requested_by to match the live PR `author_login`, the org owner `DongwonTTuna`, OR any `*"[bot]"` slug. The `*[bot]` wildcard trusts any bot slug rather than a specific allowlisted app. It was deliberately left intact because: (a) it is only reached AFTER state=OPEN + head-matches + base-matches + NON-FORK same-repo + owner=DongwonTTuna-Labs all pass, so the actor is already inside a trusted same-repo PR; (b) repository_dispatch (event_type codex-loop) can only be POSTed by a token with contents:write, so a fork contributor cannot inject a spoofed requested_by into the base repo's dispatch; (c) the continuation dispatch carries the ORIGINAL requested_by forward unchanged, and a bot-authored PR already matches author_login, so a generic *[bot] allowance is largely redundant for legitimate flows; (d) the consumer adapter (Wave 5, not yet built) is what sets the initial requested_by, so the specific legitimate bot slug cannot be verified yet, and narrowing *[bot] to an allowlist now risks breaking a legitimate bot-initiated continuation; (e) AGENTS.md forbids weakening/guessing and an existing pinned test asserts the *[bot] literal. Recommendation for a later task (once the consumer adapter exists): replace `*[bot]` with an explicit allowed-app/bot allowlist (e.g. the CODEX GitHub App bot slug) sourced from config, with a fixture proving a non-allowlisted *[bot] requester terminates as untrusted_requester. who=Task 24 scope boundary / what=*[bot] wildcard requester allowance / when=Task 24 / why=cannot verify legit bot slug pre-Wave-5 + bounded by upstream non-fork/owner guards + write-scoped dispatch / how=cross-check vs live author already present; defense-in-depth gates added on fix-push + dispatch; *[bot] tightening deferred to a config-backed allowlist task.
- 2026-06-04: OPEN (Task 24 local tooling, inherited): LSP diagnostics remain unavailable in HSI (`yaml-language-server`/`basedpyright-langserver` not installed). Task 24 used `/opt/homebrew/bin/actionlint` (1.7.12, exit 0), `uvx --with-editable setup/codex-review pytest` (focused + full, 358 passed), a rendered-if GitHub-Actions-expression simulation for fork/untrusted/trusted scenarios as manual QA, `git diff --check` (exit 0), and a generated-artifact scan/cleanup. No Python source was edited so py_compile was not required. who=local LSP tooling / what=YAML/Python language servers unavailable / when=Task 24 verification / why=servers not installed / how=actionlint + pytest + if-gate simulation + diff check + artifact scan.

- 2026-06-04: OBSERVATION (Task 25, scope boundary — NOT a defect): `empty_patch`, `no_fix_needed`, and `no_fix_changes` are classified as NON-FAILURE (run stays green) because the task instructs "do not fail LGTM, dry_run, or expected no-fix"; the task's required failure list does not include them. If policy later wants `empty_patch` treated as a failure, move it from the non-failure case label in BOTH the `terminal_state` outcome classifier and the `terminal_conclusion` gate (and update `NON_FAILURE_REASONS` in `test_terminal_visibility.py` + the docs "keep the run green" list). who=Task 25 conclusion classification / what=no-fix family treated as success / when=Task 25 / why=task spec marks expected no-fix as non-failure / how=single case label in finalize-stage; documented for easy re-tuning.
- 2026-06-04: OBSERVATION (Task 25, dispatch_failed visibility): a network failure of the continuation `curl -fsS .../dispatches` fails the "Emit repository dispatch continuation" step and turns the job red (correct, not masked), but the `terminal_state`/`terminal_conclusion` steps then see `terminal_reason=""` (continuation) and do not relabel it to canonical `dispatch_failed` in `codex-loop-state.json`. Promoting a live curl failure into a `dispatch_failed` terminal_reason in the state JSON is Task 22/23 emit-path territory and was left unchanged to honor "Do NOT alter Task 22 App-token dispatch / Task 23 ledger". The run is still visibly failed via the failing curl step + always-uploaded terminal artifact. who=Task 25 vs Task 22/23 boundary / what=curl failure not normalized to dispatch_failed reason / when=Task 25 / why=emit-path normalization is out of Task 25 scope / how=documented; job already goes red so no masking.
- 2026-06-04: OPEN (Task 25 local tooling, inherited): LSP diagnostics remain unavailable in HSI (`yaml-language-server`/`basedpyright-langserver` not installed). Task 25 verified with `/opt/homebrew/bin/actionlint`, `uvx --with-editable setup/codex-review pytest` (focused + full), a verbatim-run-script simulation harness as manual QA, `git diff --check`, and a generated-artifact scan/cleanup. No Python source changed (only the workflow YAML + a new pytest file + docs), so `py_compile` was not required. who=local LSP tooling / what=YAML/Python language servers unavailable / when=Task 25 verification / why=servers not installed / how=actionlint + pytest + run-script simulation + diff check + artifact scan.

- 2026-06-04: BLOCKED (Task 26 dry-run dispatch smoke, NOT a defect in Tasks 22-25): the documented Default-Branch Dispatch Smoke Test cannot run yet. who=PR-gated migration sequencing (the Task 5-25 pipeline is uncommitted on `work/codex-loop-reusable-workflow`; only the PR #18 scaffold is on default branch `main`) / what=preconditions unmet: (a) current pipeline is not on the default branch so a `repository_dispatch` would fire the scaffold (misleading), (b) no open same-repo dry-run test PR (`gh pr list`=[]), (c) no prior codex-loop-dispatch run so no state artifact exists for the required `state_run_id`/`state_artifact_name` pointer (`gh run list`=[]; main dispatch `grep -c state_run_id`=0) / when=Task 26 (Wave 4 closing gate), pipeline implemented but not yet merged to default branch / why=`repository_dispatch` only fires workflows on the default branch (docs:314) + Task 26 MUST-NOT forbids a doomed/misleading dispatch / how=read-only `gh` verification only, NO dispatch sent (neither dry_run true nor false); blocker + unblock handoff recorded in `.omo/evidence/task-26-smoke-blocked.txt`; deterministic negative evidence substituted in `.omo/evidence/task-26-negative.txt`. Unblock path (owned by Wave 5 + the live-capable PR, not Task 26): merge pipeline to default branch -> open same-repo non-fork dry-run test PR -> seed first state artifact via one reusable dry-run run -> dispatch with dry_run=true only and capture `gh run view --json conclusion,jobs` into task-26-smoke.json.
- 2026-06-04: OPEN (re-affirmed by Task 26, inherited from Task 25, NOT expanded): a continuation `curl -fsS .../dispatches` network failure turns the job red (visible) but is not relabeled to canonical `dispatch_failed` in `codex-loop-state.json` (the terminal_state step sees terminal_reason="" for the continuation path). Task 26 did NOT touch this because (a) no Task 26 evidence required it, (b) it is Task 22/23 emit-path territory, and (c) the run is already visibly failed via the failing curl step + always-uploaded terminal artifact, so it is not a silent loop. Left for a future targeted emit-path fix; recorded here for the Wave 5 gate and Task 27.

- 2026-06-04: BLOCKED (Task 29 live/dry-run parity, NOT a no-overlap defect): RS legacy auto label overlap is resolved in the working tree, but runtime parity could not be attempted. who=RS adapter rollout + HSI reusable pin/default-branch state / what=preconditions unmet: new RS adapters are not default-branch active (`gh workflow list` shows only Codex Review/Design/Fix/Issue legacy workflow names), pinned HSI SHA `727f713d2fc497b735ac17b575b74e282b09830e` is PR #18 scaffold and not live-capable Task 5-25 continuation, no explicit approved Task 29 live PR plus prior dry-run smoke evidence was available / when=Task 29 after disabling legacy triggers / why=dispatching now would exercise missing/default-branch-inactive adapter state or scaffold semantics and would create misleading evidence / how=read-only `gh` checks only, no `workflow_dispatch`, no `repository_dispatch`, no label/comment/issue mutation, no `dry_run=false`; blocker and unblock path recorded in `.omo/evidence/task-29-live-blocked.txt`.
