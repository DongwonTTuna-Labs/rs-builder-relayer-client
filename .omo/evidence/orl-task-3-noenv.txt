# orl-task-3-noenv

Fail-closed behavior when neither relay key source is present:

What was run in this task:

- Read `.github/workflows/grimoire-attune.yml` and confirmed the resolver maps the GitHub secret to `AI_RELAY_API_KEY_SECRET` instead of directly shadowing the runtime `AI_RELAY_API_KEY` environment variable.
- Confirmed the resolver selects `AI_RELAY_API_KEY_SECRET` first when non-empty, then runner-inherited `AI_RELAY_API_KEY` when non-empty.
- Confirmed that if both sources are empty, the workflow prints a non-secret error message and exits before the model smoke.
- Confirmed the selected value is masked before export when a source exists.

Referenced negative model-call observation instead of re-running:

- Task 2 recorded that `opencode run --format json` with no relay key available emitted a missing API key error event and did not print a key value.
- Task 2 also recorded that the command can return exit code 0 despite the JSON API error, so grimoire CI must inspect output sentinels/error events rather than trusting process status alone.

No relay key value was available or needed for this docs-only task. No secret value, token value, PAT value, private key, or runner env value was printed, captured, or committed.
