# orl-task-1-nosecret

Task: secret hygiene evidence for grimoire attune
Date: 2026-06-11

RESULT: LOCAL STATIC SECRET-HYGIENE PASS.

Scope checked before this evidence file was written:
- .github/workflows/grimoire-attune.yml

Static scan results:
- Pure-mode flag: 0 matches.
- Default Actions token auth literal/context, GitHub App credential patterns, retired Codex action/setup helper patterns, and tokenless-checkout patterns: 0 matches.
- Classic GitHub token prefix patterns: 0 matches.
- Fine-grained GitHub token prefix patterns: 0 matches.
- Private-key PEM header pattern: 0 matches.
- Raw relay-key assignment pattern in workflow logs/artifact scan target: 0 matches.

Workflow hygiene facts:
- The workflow does not print environment dumps.
- The workflow does not print the selected relay key.
- The resolver masks the selected relay value before exporting it for child processes.
- The checkout token input is PAT-only through `secrets.GRIMOIRE_PAT`; a preflight maps the secret to `GRIMOIRE_PAT_SECRET`, masks it, and fails closed before checkout if missing.
- Checkout keeps `persist-credentials: false`, so the PAT is not persisted into local git config.
- The ai-relay provider config stores only an env placeholder, never a key value.
- Generated healthcheck artifacts are scanned in-run for token-shaped output before the job can report success.

Live-log status:
- No live workflow log exists yet because dispatch was not attempted from this local-only repair step.
- This evidence therefore covers static workflow hygiene and the workflow's own future artifact scan, not a live log bundle.
