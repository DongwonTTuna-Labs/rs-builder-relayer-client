# Task 10 RS Adapter Re-pin Evidence

Timestamp: 2026-06-04T16:16:47Z

## Files updated

- `.github/workflows/codex-loop-review-adapter.yml`
- `.github/workflows/codex-loop-manual-adapter.yml`
- `.github/workflows/codex-loop-dispatch-adapter.yml`

## Before and after reusable refs

Before all three adapters:

```text
DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@727f713d2fc497b735ac17b575b74e282b09830e
```

After all three adapters:

```text
DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@3540af40d3ef8614ec978a4de90caa69b47f3b99
```

Ref search command:

```bash
rg -n "727f713d2fc497b735ac17b575b74e282b09830e|codex-loop-reusable.yml@" .github/workflows/codex-loop-review-adapter.yml .github/workflows/codex-loop-manual-adapter.yml .github/workflows/codex-loop-dispatch-adapter.yml
```

Result:

```text
.github/workflows/codex-loop-dispatch-adapter.yml:46:    uses: DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@3540af40d3ef8614ec978a4de90caa69b47f3b99
.github/workflows/codex-loop-manual-adapter.yml:77:    uses: DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@3540af40d3ef8614ec978a4de90caa69b47f3b99
.github/workflows/codex-loop-review-adapter.yml:35:    uses: DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@3540af40d3ef8614ec978a4de90caa69b47f3b99
```

No stale `727f713d2fc497b735ac17b575b74e282b09830e` reference remains in the three Codex loop adapters.

## Explicit secret mapping proof

All three adapter reusable calls now use:

```yaml
secrets:
  CODEX_GITHUB_APP_ID: ${{ secrets.CODEX_APP_ID }}
  CODEX_GITHUB_APP_PRIVATE_KEY: ${{ secrets.CODEX_APP_PRIVATE_KEY }}
```

Static assertion command:

```bash
python3 - <<'PY'
from pathlib import Path
files=[
'.github/workflows/codex-loop-review-adapter.yml',
'.github/workflows/codex-loop-manual-adapter.yml',
'.github/workflows/codex-loop-dispatch-adapter.yml',
]
target='DongwonTTuna-Labs/home-server-infra/.github/workflows/codex-loop-reusable.yml@3540af40d3ef8614ec978a4de90caa69b47f3b99'
stale='727f713d2fc497b735ac17b575b74e282b09830e'
for f in files:
    s=Path(f).read_text()
    assert target in s, f'{f}: missing target pin'
    assert stale not in s, f'{f}: stale pin remains'
    assert 'secrets: inherit' not in s, f'{f}: secrets inherit remains'
    assert 'CODEX_GITHUB_APP_ID: ${{ secrets.CODEX_APP_ID }}' in s, f'{f}: app id mapping missing'
    assert 'CODEX_GITHUB_APP_PRIVATE_KEY: ${{ secrets.CODEX_APP_PRIVATE_KEY }}' in s, f'{f}: private key mapping missing'
print('static assertions passed for all three adapters')
PY
```

Result:

```text
static assertions passed for all three adapters
```

## HSI reusable secret contract cross-check

Command:

```bash
python3 - <<'PY'
import subprocess, re
p='repos/DongwonTTuna-Labs/home-server-infra/contents/.github/workflows/codex-loop-reusable.yml?ref=3540af40d3ef8614ec978a4de90caa69b47f3b99'
text=subprocess.check_output(['gh','api','-H','Accept: application/vnd.github.raw',p], text=True)
print(sorted(set(re.findall(r'secrets\\.([A-Z0-9_]+)', text))))
PY
```

Result:

```text
['CODEX_GITHUB_APP_ID', 'CODEX_GITHUB_APP_PRIVATE_KEY']
```

Declared `workflow_call.secrets` block at the target SHA contains only `CODEX_GITHUB_APP_ID` and `CODEX_GITHUB_APP_PRIVATE_KEY`, both `required: false`. No additional declared or referenced reusable secrets were found.

## actionlint

Command:

```bash
actionlint .github/workflows/codex-loop-review-adapter.yml .github/workflows/codex-loop-manual-adapter.yml .github/workflows/codex-loop-dispatch-adapter.yml
```

Result: passed with no output.

## git diff check

Command:

```bash
git diff --check
```

Result: passed with no output.

## LSP diagnostics note

`lsp_diagnostics` was attempted for all three changed YAML files, but `yaml-language-server` is not installed in this environment. `actionlint` and static assertions passed.
