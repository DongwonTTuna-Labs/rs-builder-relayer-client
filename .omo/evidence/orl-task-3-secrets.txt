# orl-task-3-secrets

Command run locally:

```text
gh secret list
```

Name-only findings for required grimoire secrets:

- `GRIMOIRE_PAT`: present as a repository secret.
- `AI_RELAY_API_KEY`: not present as a repository secret; this is intentional for the current setup because the self-hosted runner supplies `AI_RELAY_API_KEY` through its Docker environment.

Observed relevant auth reality:

- ai-relay key is dual-source: `secrets.AI_RELAY_API_KEY` if registered, otherwise runner-inherited `AI_RELAY_API_KEY`.
- PAT auth is repo secret first: `GRIMOIRE_PAT`; self-hosted runners may provide fallback `CODEX_LOOP_PAT`.

No secret values were requested, printed, or recorded.
