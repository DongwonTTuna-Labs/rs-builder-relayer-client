#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SETUP_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
export PYTHONPATH="${SETUP_ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m codex_review.cli "$@"
