Create codex.stage03.model_design.v1 JSON for the blocking findings.
The PR head checkout is in workspace; inspect review target files and diffs through that path.
test_plan entries must be exact commands: one command per entry, no markdown, no backticks, no prose.
Use exact commands such as cargo fmt --all --check, cargo clippy --workspace --all-targets --all-features -- -D warnings, cargo test --workspace --all-features, python3 -m unittest discover -s setup/codex-review/tests/unit, python3 -m unittest discover -s setup/codex-review/tests/workflow, git diff --check.
